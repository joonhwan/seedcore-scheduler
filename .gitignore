# dependencies
node_modules/
.pnpm-store/

# build output
dist/
dist-exe/
dist-bundle/
# 고객사 전달용 배포 zip (하나가 150MB 대). build-exe.js 7단계가 여기에 쌓는다.
dist-release/
build/
.next/
.turbo/
*.tsbuildinfo
apps/api/public/

# tsc -b 가 워크스페이스 *.config.ts 와 같은 위치에 떨어뜨리는 산출물
# (실제 런타임은 원본 .ts 를 읽음)
apps/web/vite.config.js
apps/web/vite.config.d.ts
apps/web/tailwind.config.js
apps/web/tailwind.config.d.ts

# tsc -w 가 incremental 캐시 꼬임으로 src/ 에 emit 하는 케이스
packages/shared/src/*.js
packages/shared/src/*.js.map

# env / secrets
.env
.env.local
.env.*.local

# data / runtime (local dev & exe)
data/
*.db
*.db-shm
*.db-wal
backup/
backups/
logs/


# editor / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db

# coverage
coverage/

# Playwright MCP scratch (browser snapshots / console logs)
.playwright-mcp/

# Superpowers SDD 작업 공간 (ledger / brief / review package 등 임시 산출물)
.superpowers/

# Claude Code — 개인 설정만 제외한다.
# .claude/skills/ 는 커밋한다: AGENTS.md §5 가 sam-troubleshooting 스킬을 가리키고 있어서,
# 스킬 파일이 저장소에 없으면 그 안내가 빈 포인터가 된다.
.claude/settings.local.json

# 세션 이관 노트 (개인 작업 메모)
.remember/
