# 사용자 완전 삭제와 퇴사 처리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 활동 기록이 없는 계정을 완전히 지우고, 활동이 있는 계정은 퇴사 처리로 목록에서 감추면서 되돌릴 수 있게 한다.

**Architecture:** 퇴사는 `users.retired_at` 을 채우면서 `is_active` 를 함께 내리는 방식이다. `is_active` 를 보고 사람을 걸러내는 서버 코드 열다섯 군데가 그대로 퇴사자를 막으므로 그 자리들을 손대지 않는다. 삭제는 `User` 를 참조하는 아홉 갈래를 세어 모두 0일 때만 허용하고, 감사로그는 행을 남긴 채 행위자만 비운다. 집계를 세는 함수 하나를 조회와 삭제 직전 재확인이 함께 쓴다.

**Tech Stack:** Node.js 22.13+, NestJS 10, Prisma 5.22, SQLite(WAL), React 18, Vite 5, TanStack Query, Tailwind, Zod, vitest

**Spec:** `docs/superpowers/specs/2026-09-04-사용자삭제-퇴사처리-design.md`

## Global Constraints

이 절의 항목은 **모든 과제의 요구사항에 암묵적으로 포함된다.**

- **`cd` 를 독립적으로 실행하지 않는다.** 절대 경로를 쓰거나 도구의 작업 디렉터리를 지정한다. 워크스페이스 명령은 저장소 루트에서 `pnpm -F <pkg> ...` 로 실행한다.
- **기존 주석을 지우거나 바꾸지 않는다.** 인라인 주석, 설계 마일스톤 설명, docstring 은 기능 변경과 직접 관계가 없는 한 그대로 둔다.
- **검증은 Zod 스키마와 `ZodValidationPipe` 로만 한다.** `class-validator` 와 `ValidationPipe` 는 이 저장소에서 쓰지 않는다.
- **상태를 바꾸는 라우트에 `@UseGuards(OriginGuard)` 가 걸려 있어야 한다.** `UsersController` 는 클래스 단위로 `@UseGuards(OriginGuard)` 와 `@AdminOnly()` 를 이미 달고 있으므로 새 라우트가 그대로 상속받는다. **메서드마다 다시 붙이지 않는다.**
- **`exactOptionalPropertyTypes: true`** 이므로 optional 필드에 `undefined` 를 직접 넣지 않는다. 비구조화 할당이나 조건부 전개로 분기한다.
- **이 작업에 마이그레이션은 없다.** `users.retired_at` 은 2차 마이그레이션(`20260903010359_user_groups_and_retired_at`)에 이미 들어가 있고 운영에도 적용되었다. **`schema.prisma` 를 건드리지 않는다.**
- **`packages/shared` 에 새 export 를 추가한 뒤에는 `pnpm -F @sam/shared build` 를 돌린다.** 개발 서버는 `pnpm dev` 로 띄운다(`scripts/dev.mjs` 가 vite 사전 번들 캐시를 매번 지운다). 다른 방법으로 띄웠다면 `apps/web/node_modules/.vite` 를 손으로 지운다.
- **코드를 고친 뒤에는 `pnpm -r typecheck` 를 돌린다.**
- **커밋 메시지는 파일에 써서 `git commit -F` 로 넘긴다.** PowerShell 문법(`@'...'@`)을 Bash 도구에 쓰면 제목에 `@` 가 섞인다. 커밋 메시지 제목은 한국어 평서형 종결(`~한다`)로 쓴다.
- **웹에는 React 컴포넌트 테스트 환경이 없다.** `apps/web/vitest.config.ts` 는 `environment: 'node'` 이고 `src/**/*.test.ts` 만 포함하므로, 화면 과제의 검증은 `pnpm -r typecheck` 와 직접 조작으로 한다.

---

## File Structure

### 새로 만드는 파일

| 경로 | 책임 |
| --- | --- |
| `apps/api/src/users/users.service.test.ts` | 집계·퇴사·복직·삭제의 거부 규칙과 동작을 고정하는 단위 시험 |
| `apps/web/src/components/UserDeleteConfirmDialog.tsx` | 아이디 입력을 요구하는 삭제 확인 대화상자 |

### 고치는 파일

| 경로 | 무엇을 |
| --- | --- |
| `packages/shared/src/index.ts` | `UserListItem.retiredAt`, `UserActivitySummary`, `AuditAction` 셋 |
| `apps/api/src/users/users.service.ts` | `countActivity`·`activity`·`retire`·`unretire`·`remove`, `list` 의 `includeRetired`, 응답 매핑 헬퍼 |
| `apps/api/src/users/users.controller.ts` | 라우트 넷과 `list` 의 쿼리 파라미터 |
| `apps/web/src/lib/errors.ts` | 새 오류 코드 넷의 한국어 문장 |
| `apps/web/src/lib/users.ts` | `includeRetired`, `useUserActivity`, `useRetireUser`, `useUnretireUser`, `useDeleteUser` |
| `apps/web/src/pages/AdminUsersPage.tsx` | "퇴사자 포함" 체크상자, 퇴사 배지, 퇴사자 행의 토글 감추기 |
| `apps/web/src/pages/AdminUserDetailPage.tsx` | "계정 정리" 섹션 |

---

## Task 1: 공유 타입과 오류 문구

**Files:**

- Modify: `packages/shared/src/index.ts`
- Modify: `apps/web/src/lib/errors.ts`

**Interfaces:**

- Consumes: 없음 (첫 과제)
- Produces: `UserActivitySummary` 타입, `UserListItem.retiredAt: string | null`, `AuditAction` 에 `'USER_RETIRE' | 'USER_UNRETIRE' | 'USER_DELETE'`

- [ ] **Step 1: `UserListItem` 에 `retiredAt` 을 더한다**

`packages/shared/src/index.ts` 의 `UserListItem`(125행 부근)에서 `lastLoginAt` 다음 줄에 넣는다.

```ts
export const UserListItem = z.object({
  id: z.string(),
  username: z.string(),
  displayName: z.string(),
  globalRole: GlobalRole,
  isActive: z.boolean(),
  passwordMustChange: z.boolean(),
  lockedUntil: z.string().nullable(), // ISO datetime
  failedLoginCount: z.number().int(),
  lastLoginAt: z.string().nullable(),
  /** 퇴사 처리 시각. 비어 있으면 재직. 값이 있으면 isActive 는 반드시 false 다. */
  retiredAt: z.string().nullable(),
  createdAt: z.string(),
});
export type UserListItem = z.infer<typeof UserListItem>;
```

- [ ] **Step 2: `UserActivitySummary` 를 더한다**

같은 파일의 `ResetPasswordResponse` 정의 바로 다음에 넣는다.

```ts
/**
 * 계정 하나가 남긴 활동의 집계. 완전 삭제가 가능한지 판단하는 근거다.
 *
 * `clearable` 과 `permanent` 로 나눈 이유는 관리자가 무엇을 하면 지울 수 있는지 알려 주기
 * 위함이다. 앞의 둘은 화면에서 빼면 0 이 되지만, 뒤의 일곱은 영구히 남으므로 그 계정은
 * 퇴사 처리만 할 수 있다. 화면이 "일정 47건을 수정하고 댓글 5건을 남긴" 같은 문구를 만들 수
 * 있도록 합계가 아니라 항목별 건수를 그대로 내린다.
 */
export const UserActivitySummary = z.object({
  /** 아래 아홉 갈래가 모두 0 인가. */
  canDelete: z.boolean(),
  /** 관리자가 정리하면 없어지는 것. */
  clearable: z.object({
    projectMemberships: z.number().int(),
    groupMemberships: z.number().int(),
  }),
  /** 지울 수 없는 것. 하나라도 있으면 그 계정은 영구히 삭제할 수 없다. */
  permanent: z.object({
    createdProjects: z.number().int(),
    nodesCreated: z.number().int(),
    nodesUpdated: z.number().int(),
    comments: z.number().int(),
    history: z.number().int(),
    membershipsAdded: z.number().int(),
    groupMembersAdded: z.number().int(),
  }),
});
export type UserActivitySummary = z.infer<typeof UserActivitySummary>;
```

- [ ] **Step 3: 감사 액션 셋을 더한다**

같은 파일의 `AuditAction`(145행 부근)에서 `'USER_UNLOCK'` 다음 줄에 넣는다. 사용자 관련 액션끼리 모아 두기 위함이다.

```ts
  'USER_UNLOCK',
  'USER_RETIRE',
  'USER_UNRETIRE',
  'USER_DELETE',
  'ADMIN_OVERRIDE_EDIT',
```

- [ ] **Step 4: 오류 문구 넷을 더한다**

`apps/web/src/lib/errors.ts` 의 `KNOWN` 에서 `LAST_ACTIVE_ADMIN` 다음 줄에 넣는다.

```ts
  LAST_ACTIVE_ADMIN: '활성 ADMIN 이 자기 자신뿐이라 비활성화할 수 없습니다.',
  SELF_ACTION_FORBIDDEN: '자기 자신의 계정에는 할 수 없는 작업입니다.',
  ALREADY_RETIRED: '이미 퇴사 처리된 계정입니다.',
  NOT_RETIRED: '퇴사 처리된 계정이 아닙니다.',
  USER_HAS_ACTIVITY:
    '활동 기록이 남아 있어 삭제할 수 없습니다. 화면을 새로 읽어 현재 상태를 확인하십시오.',
```

- [ ] **Step 5: 빌드하고 어디가 깨지는지 확인한다**

```bash
pnpm -F @sam/shared build
pnpm -r typecheck
```

기대: **`apps/api` 에서 타입 오류가 난다.** `UserListItem` 에 필수 필드를 더했으므로 `users.service.ts` 의 응답 매핑 세 곳(`list`·`create`·`update`)이 `retiredAt` 을 빠뜨렸다고 알려 준다. 이 오류는 Task 2 에서 매핑 헬퍼를 만들며 함께 없앤다. **여기서 급히 세 곳에 `retiredAt` 을 손으로 끼워 넣지 말 것** — 다음 과제가 그 반복을 한 곳으로 모은다.

- [ ] **Step 6: 커밋**

```bash
git add packages/shared/src/index.ts apps/web/src/lib/errors.ts
git commit -F <메시지 파일>
```

메시지:

```
feat(shared): 퇴사 시각과 활동 집계 타입을 더한다

UserListItem 에 retiredAt 을 더하고, 완전 삭제 가능 여부를 판단하는
UserActivitySummary 를 정의한다. 집계를 clearable 과 permanent 로 나눈 것은
관리자가 무엇을 하면 지울 수 있는지 화면이 안내할 수 있게 하기 위함이다.

감사 액션 셋(USER_RETIRE·USER_UNRETIRE·USER_DELETE)과 오류 문구 넷을 함께 더한다.

이 커밋만으로는 apps/api 의 타입 검사가 통과하지 않는다. UserListItem 에 필수
필드가 늘었으므로 응답 매핑 세 곳이 깨지며, 다음 커밋이 그것을 한 곳으로 모아
해소한다.
```

---

## Task 2: 활동 집계 (`countActivity` 와 `activity`)

**Files:**

- Modify: `apps/api/src/users/users.service.ts`
- Create: `apps/api/src/users/users.service.test.ts`

**Interfaces:**

- Consumes: Task 1 의 `UserActivitySummary`, `UserListItem.retiredAt`
- Produces:
  - `UsersService.activity(id: string): Promise<UserActivitySummary>`
  - `private countActivity(id: string, client: Prisma.TransactionClient): Promise<{ summary: UserActivitySummary; total: number }>` — Task 4 의 삭제가 트랜잭션 안에서 다시 부른다
  - `toUserListItem(u: User): UserListItem` — 파일 하단의 모듈 함수. `list`·`create`·`update`·`retire`·`unretire` 가 모두 쓴다

- [ ] **Step 1: 실패하는 시험을 쓴다**

`apps/api/src/users/users.service.test.ts` 를 새로 만든다. Prisma 대역은 `user-projects.service.test.ts` 와 같은 방식(`vi.fn()` 으로 필요한 메서드만 흉내)이다.

```ts
import { describe, expect, it, vi } from 'vitest';
import { UsersService } from './users.service';
import type { PrismaService } from '../prisma/prisma.service';
import type { AuthService } from '../auth/auth.service';
import type { SessionsService } from '../sessions/sessions.service';
import type { AuditService } from '../audit/audit.service';

const T0 = new Date('2026-09-01T00:00:00.000Z');

interface UserRow {
  id: string;
  username: string;
  displayName: string;
  passwordHash: string;
  passwordMustChange: boolean;
  globalRole: string;
  isActive: boolean;
  preferencesJson: string | null;
  createdAt: Date;
  updatedAt: Date;
  lastLoginAt: Date | null;
  failedLoginCount: number;
  lockedUntil: Date | null;
  retiredAt: Date | null;
}

function userRow(over: Partial<UserRow> & { id: string }): UserRow {
  return {
    username: over.id,
    displayName: over.id,
    passwordHash: 'x',
    passwordMustChange: false,
    globalRole: 'USER',
    isActive: true,
    preferencesJson: null,
    createdAt: T0,
    updatedAt: T0,
    lastLoginAt: null,
    failedLoginCount: 0,
    lockedUntil: null,
    retiredAt: null,
    ...over,
  };
}

/**
 * 아홉 갈래의 건수를 통째로 받아 대역을 만든다. 키를 빠뜨리면 0 으로 본다.
 */
interface Counts {
  projectMemberships?: number;
  groupMemberships?: number;
  createdProjects?: number;
  nodesCreated?: number;
  nodesUpdated?: number;
  comments?: number;
  history?: number;
  membershipsAdded?: number;
  groupMembersAdded?: number;
}

function buildService(seed: { users?: UserRow[]; counts?: Counts } = {}) {
  const users = [...(seed.users ?? [userRow({ id: 'u1' })])];
  const c = seed.counts ?? {};
  const auditRows: { actorId: string | null }[] = [];
  const deleted: string[] = [];

  const prismaObject = {
    user: {
      findUnique: vi.fn(async ({ where }: { where: { id: string } }) =>
        users.find((u) => u.id === where.id) ?? null,
      ),
      findMany: vi.fn(async () => users),
      count: vi.fn(
        async ({ where }: { where: { globalRole?: string; isActive?: boolean; id?: { not: string } } }) =>
          users.filter(
            (u) =>
              (where.globalRole === undefined || u.globalRole === where.globalRole) &&
              (where.isActive === undefined || u.isActive === where.isActive) &&
              (where.id === undefined || u.id !== where.id.not),
          ).length,
      ),
      update: vi.fn(
        async ({ where, data }: { where: { id: string }; data: Partial<UserRow> }) => {
          const row = users.find((u) => u.id === where.id)!;
          Object.assign(row, data);
          return row;
        },
      ),
      delete: vi.fn(async ({ where }: { where: { id: string } }) => {
        const i = users.findIndex((u) => u.id === where.id);
        deleted.push(where.id);
        return users.splice(i, 1)[0]!;
      }),
    },
    projectMember: {
      count: vi.fn(async ({ where }: { where: { userId?: string; addedById?: string } }) =>
        where.addedById !== undefined ? (c.membershipsAdded ?? 0) : (c.projectMemberships ?? 0),
      ),
    },
    userGroupMember: {
      count: vi.fn(async ({ where }: { where: { userId?: string; addedById?: string } }) =>
        where.addedById !== undefined ? (c.groupMembersAdded ?? 0) : (c.groupMemberships ?? 0),
      ),
    },
    project: { count: vi.fn(async () => c.createdProjects ?? 0) },
    scheduleNode: {
      count: vi.fn(async ({ where }: { where: { createdById?: string; updatedById?: string } }) =>
        where.updatedById !== undefined ? (c.nodesUpdated ?? 0) : (c.nodesCreated ?? 0),
      ),
    },
    nodeComment: { count: vi.fn(async () => c.comments ?? 0) },
    nodeHistory: { count: vi.fn(async () => c.history ?? 0) },
    auditLog: {
      updateMany: vi.fn(async () => {
        auditRows.push({ actorId: null });
        return { count: 1 };
      }),
    },
    $transaction: vi.fn(async (fn: (tx: unknown) => Promise<unknown>) => fn(prismaObject)),
  };

  const prisma = prismaObject as unknown as PrismaService;
  const auth = {} as AuthService;
  const sessions = { destroyAllForUser: vi.fn(async () => 2) } as unknown as SessionsService;
  const audit = { log: vi.fn(async () => undefined) } as unknown as AuditService;

  return {
    service: new UsersService(prisma, auth, sessions, audit),
    prismaObject,
    sessions,
    audit,
    users,
    deleted,
    auditRows,
  };
}

describe('UsersService.activity()', () => {
  it('아홉 갈래가 모두 0 이면 삭제할 수 있다', async () => {
    const { service } = buildService();
    const result = await service.activity('u1');
    expect(result.canDelete).toBe(true);
    expect(result.clearable).toEqual({ projectMemberships: 0, groupMemberships: 0 });
  });

  it('정리하면 없어지는 것만 있어도 삭제할 수 없다', async () => {
    const { service } = buildService({ counts: { projectMemberships: 3, groupMemberships: 1 } });
    const result = await service.activity('u1');
    expect(result.canDelete).toBe(false);
    expect(result.clearable).toEqual({ projectMemberships: 3, groupMemberships: 1 });
    expect(result.permanent.nodesUpdated).toBe(0);
  });

  it('남을 추가한 기록도 센다', async () => {
    const { service } = buildService({ counts: { membershipsAdded: 4, groupMembersAdded: 2 } });
    const result = await service.activity('u1');
    expect(result.canDelete).toBe(false);
    expect(result.permanent.membershipsAdded).toBe(4);
    expect(result.permanent.groupMembersAdded).toBe(2);
  });

  it('일정 생성과 수정을 따로 센다', async () => {
    const { service } = buildService({ counts: { nodesCreated: 7, nodesUpdated: 47 } });
    const result = await service.activity('u1');
    expect(result.permanent.nodesCreated).toBe(7);
    expect(result.permanent.nodesUpdated).toBe(47);
  });

  it('없는 사용자는 USER_NOT_FOUND 다', async () => {
    const { service } = buildService();
    await expect(service.activity('missing')).rejects.toMatchObject({
      response: { error: 'USER_NOT_FOUND' },
    });
  });
});
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
```

기대: FAIL. `service.activity is not a function`.

- [ ] **Step 3: 응답 매핑을 한 곳으로 모은다**

`apps/api/src/users/users.service.ts` 파일 하단(`generateTemporaryPassword` 앞)에 모듈 함수를 더한다.

```ts
/**
 * Prisma 의 사용자 행을 API 응답으로 바꾼다.
 *
 * list·create·update·retire·unretire 다섯 곳이 같은 매핑을 하므로 한 곳에 모은다.
 * 필드가 하나 늘 때마다 다섯 곳을 고치다 한 곳을 빠뜨리는 일을 막기 위함이다.
 */
function toUserListItem(u: {
  id: string;
  username: string;
  displayName: string;
  globalRole: string;
  isActive: boolean;
  passwordMustChange: boolean;
  lockedUntil: Date | null;
  failedLoginCount: number;
  lastLoginAt: Date | null;
  retiredAt: Date | null;
  createdAt: Date;
}): UserListItem {
  return {
    id: u.id,
    username: u.username,
    displayName: u.displayName,
    globalRole: u.globalRole === 'ADMIN' ? 'ADMIN' : 'USER',
    isActive: u.isActive,
    passwordMustChange: u.passwordMustChange,
    lockedUntil: u.lockedUntil ? u.lockedUntil.toISOString() : null,
    failedLoginCount: u.failedLoginCount,
    lastLoginAt: u.lastLoginAt ? u.lastLoginAt.toISOString() : null,
    retiredAt: u.retiredAt ? u.retiredAt.toISOString() : null,
    createdAt: u.createdAt.toISOString(),
  };
}
```

이어서 `list`·`create`·`update` 의 반환문을 이 함수 호출로 바꾼다. `list` 는 `return users.map(toUserListItem);` 이 되고, `create` 와 `update` 는 각각 `return toUserListItem(created);`, `return toUserListItem(updated);` 가 된다. **`create` 의 지역 변수 이름을 실제 코드에서 확인해 맞춘다.**

- [ ] **Step 4: 집계 함수와 조회 메서드를 쓴다**

같은 파일에 임포트를 더한다.

```ts
import type { Prisma } from '@prisma/client';
import {
  validatePassword,
  type UserActivitySummary,
  type UserListItem,
} from '@sam/shared';
```

`unlock()` 다음에 메서드 둘을 더한다.

```ts
  /**
   * 계정 하나가 남긴 활동을 센다.
   *
   * `client` 를 받는 이유는 삭제가 트랜잭션 안에서 이 함수를 다시 부르기 때문이다. 조회
   * 시점과 삭제 시점 사이에 그 사람이 프로젝트에 추가될 수 있으므로, 지우기 직전에 같은
   * 기준으로 한 번 더 센다(설계 문서 §5.3).
   *
   * 세는 것은 아홉 갈래다. `sessions` 는 계정을 지우면 Cascade 로 함께 사라지고,
   * `audit_logs` 는 행을 남긴 채 행위자만 비우므로 둘 다 세지 않는다(설계 문서 §5.1).
   */
  private async countActivity(
    id: string,
    client: Prisma.TransactionClient,
  ): Promise<{ summary: UserActivitySummary; total: number }> {
    const [
      projectMemberships,
      groupMemberships,
      createdProjects,
      nodesCreated,
      nodesUpdated,
      comments,
      history,
      membershipsAdded,
      groupMembersAdded,
    ] = await Promise.all([
      client.projectMember.count({ where: { userId: id } }),
      client.userGroupMember.count({ where: { userId: id } }),
      client.project.count({ where: { createdById: id } }),
      client.scheduleNode.count({ where: { createdById: id } }),
      client.scheduleNode.count({ where: { updatedById: id } }),
      client.nodeComment.count({ where: { authorId: id } }),
      client.nodeHistory.count({ where: { actorId: id } }),
      client.projectMember.count({ where: { addedById: id } }),
      client.userGroupMember.count({ where: { addedById: id } }),
    ]);

    const clearable = { projectMemberships, groupMemberships };
    const permanent = {
      createdProjects,
      nodesCreated,
      nodesUpdated,
      comments,
      history,
      membershipsAdded,
      groupMembersAdded,
    };
    const total =
      Object.values(clearable).reduce((a, b) => a + b, 0) +
      Object.values(permanent).reduce((a, b) => a + b, 0);

    return { summary: { canDelete: total === 0, clearable, permanent }, total };
  }

  async activity(id: string): Promise<UserActivitySummary> {
    const target = await this.prisma.user.findUnique({ where: { id } });
    if (!target) throw new NotFoundException({ error: 'USER_NOT_FOUND' });

    const { summary } = await this.countActivity(id, this.prisma);
    return summary;
  }
```

`this.prisma` 를 `Prisma.TransactionClient` 자리에 넘길 수 있는 이유는 `PrismaService` 가 `PrismaClient` 를 확장하기 때문이다.

- [ ] **Step 5: 시험이 통과하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
pnpm -r typecheck
```

기대: 다섯 개 모두 PASS. Task 1 에서 났던 `retiredAt` 타입 오류도 함께 사라진다.

- [ ] **Step 6: 커밋**

메시지:

```
feat(api): 계정의 활동을 아홉 갈래로 세는 집계를 더한다

완전 삭제가 가능한지 판단하려면 User 를 참조하는 관계가 모두 비어 있어야 한다.
확정명세는 네 갈래만 세었으나 실제로는 아홉 갈래다. 남을 프로젝트나 그룹에 넣은
기록(added_by)이 필수 컬럼이라 빠뜨릴 수 없기 때문이다.

집계 함수가 client 를 받는 것은 삭제가 트랜잭션 안에서 같은 기준으로 다시 세기
위함이다. 조회와 삭제 사이에 그 사람이 프로젝트에 추가될 수 있다.

응답 매핑을 toUserListItem 한 곳으로 모았다. 다섯 곳이 같은 일을 하던 것이라
필드가 늘 때마다 한 곳을 빠뜨릴 자리였다.
```

---

## Task 3: 퇴사 처리와 복직

**Files:**

- Modify: `apps/api/src/users/users.service.ts`
- Modify: `apps/api/src/users/users.service.test.ts`

**Interfaces:**

- Consumes: Task 2 의 `toUserListItem`
- Produces:
  - `UsersService.retire(id: string, ctx: ActorContext): Promise<UserListItem>`
  - `UsersService.unretire(id: string, ctx: ActorContext): Promise<UserListItem>`

- [ ] **Step 1: 실패하는 시험을 더한다**

`users.service.test.ts` 의 끝에 붙인다.

```ts
const CTX = { actorId: 'admin1', ip: '127.0.0.1', userAgent: 'test' };

describe('UsersService.retire()', () => {
  it('retired_at 과 is_active 를 함께 바꾸고 세션을 끊는다', async () => {
    const { service, sessions, audit, users } = buildService({
      users: [userRow({ id: 'u1' }), userRow({ id: 'admin1', globalRole: 'ADMIN' })],
    });

    const result = await service.retire('u1', CTX);

    expect(result.retiredAt).not.toBeNull();
    expect(result.isActive).toBe(false);
    expect(users.find((u) => u.id === 'u1')!.retiredAt).not.toBeNull();
    expect(sessions.destroyAllForUser).toHaveBeenCalledWith('u1');
    expect(audit.log).toHaveBeenCalledWith(
      expect.objectContaining({
        action: 'USER_RETIRE',
        payload: { wasActive: true, sessionsKilled: 2 },
      }),
    );
  });

  it('퇴사 직전에 비활성이던 것을 wasActive 로 남긴다', async () => {
    const { service, audit } = buildService({
      users: [userRow({ id: 'u1', isActive: false }), userRow({ id: 'admin1', globalRole: 'ADMIN' })],
    });

    await service.retire('u1', CTX);

    expect(audit.log).toHaveBeenCalledWith(
      expect.objectContaining({ payload: { wasActive: false, sessionsKilled: 2 } }),
    );
  });

  it('자기 자신은 퇴사시킬 수 없다', async () => {
    const { service } = buildService({ users: [userRow({ id: 'admin1', globalRole: 'ADMIN' })] });
    await expect(service.retire('admin1', CTX)).rejects.toMatchObject({
      response: { error: 'SELF_ACTION_FORBIDDEN' },
    });
  });

  it('이미 퇴사한 사람은 다시 퇴사시킬 수 없다', async () => {
    const { service } = buildService({
      users: [userRow({ id: 'u1', isActive: false, retiredAt: T0 })],
    });
    await expect(service.retire('u1', CTX)).rejects.toMatchObject({
      response: { error: 'ALREADY_RETIRED' },
    });
  });

  it('활성 ADMIN 이 자기 혼자면 그 계정을 퇴사시킬 수 없다', async () => {
    const { service } = buildService({
      users: [userRow({ id: 'onlyAdmin', globalRole: 'ADMIN' }), userRow({ id: 'admin1' })],
    });
    await expect(service.retire('onlyAdmin', CTX)).rejects.toMatchObject({
      response: { error: 'LAST_ACTIVE_ADMIN' },
    });
  });
});

describe('UsersService.unretire()', () => {
  it('retired_at 을 비우고 활성으로 되돌린다', async () => {
    const { service, audit, users } = buildService({
      users: [userRow({ id: 'u1', isActive: false, retiredAt: T0 })],
    });

    const result = await service.unretire('u1', CTX);

    expect(result.retiredAt).toBeNull();
    expect(result.isActive).toBe(true);
    expect(users.find((u) => u.id === 'u1')!.retiredAt).toBeNull();
    expect(audit.log).toHaveBeenCalledWith(
      expect.objectContaining({ action: 'USER_UNRETIRE' }),
    );
  });

  it('재직 중인 사람은 복직시킬 수 없다', async () => {
    const { service } = buildService({ users: [userRow({ id: 'u1' })] });
    await expect(service.unretire('u1', CTX)).rejects.toMatchObject({
      response: { error: 'NOT_RETIRED' },
    });
  });
});
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
```

기대: FAIL. `service.retire is not a function`.

- [ ] **Step 3: 두 메서드를 쓴다**

`activity()` 다음에 더한다.

```ts
  /**
   * 퇴사 처리. **`retired_at` 과 `is_active` 를 반드시 함께 바꾼다.**
   *
   * `is_active` 를 보고 사람을 걸러내는 자리가 서버에 열다섯 군데 있다(로그인, 세션 검증,
   * 참여자 후보, 그룹 인원 추가 등). 함께 내리면 그 코드가 그대로 퇴사자를 막으므로 한 군데를
   * 빠뜨려 퇴사자가 참여자 후보에 뜨는 사고가 구조적으로 불가능해진다(설계 문서 §3).
   *
   * 그래서 `retired_at` 이 채워졌는데 `is_active = true` 인 조합은 만들지 않는다. 화면도
   * 퇴사자 행에서는 활성 토글을 감춘다.
   */
  async retire(id: string, ctx: ActorContext): Promise<UserListItem> {
    const target = await this.prisma.user.findUnique({ where: { id } });
    if (!target) throw new NotFoundException({ error: 'USER_NOT_FOUND' });
    if (id === ctx.actorId) {
      throw new BadRequestException({ error: 'SELF_ACTION_FORBIDDEN' });
    }
    if (target.retiredAt !== null) {
      throw new BadRequestException({ error: 'ALREADY_RETIRED' });
    }

    // 단일 ADMIN 보호 — update() 의 비활성화 금지와 같은 규칙이다.
    if (target.globalRole === 'ADMIN' && target.isActive) {
      const otherActiveAdmins = await this.prisma.user.count({
        where: { globalRole: 'ADMIN', isActive: true, id: { not: id } },
      });
      if (otherActiveAdmins === 0) {
        throw new BadRequestException({ error: 'LAST_ACTIVE_ADMIN' });
      }
    }

    const updated = await this.prisma.user.update({
      where: { id },
      data: { retiredAt: new Date(), isActive: false },
    });
    const sessionsKilled = await this.sessions.destroyAllForUser(id);

    await this.audit.log({
      actorId: ctx.actorId,
      action: 'USER_RETIRE',
      targetType: 'user',
      targetId: id,
      ip: ctx.ip,
      userAgent: ctx.userAgent,
      payload: { wasActive: target.isActive, sessionsKilled },
    });

    return toUserListItem(updated);
  }

  /**
   * 복직. **항상 활성으로 되돌린다.**
   *
   * 퇴사 전에 비활성이던 사람이라면 관리자가 복직 후 다시 내리면 된다. 드문 경우를 위해
   * 전이를 복잡하게 만들지 않는다. 퇴사 직전의 상태는 USER_RETIRE 감사로그의 wasActive 에
   * 남아 있다.
   */
  async unretire(id: string, ctx: ActorContext): Promise<UserListItem> {
    const target = await this.prisma.user.findUnique({ where: { id } });
    if (!target) throw new NotFoundException({ error: 'USER_NOT_FOUND' });
    if (target.retiredAt === null) {
      throw new BadRequestException({ error: 'NOT_RETIRED' });
    }

    const updated = await this.prisma.user.update({
      where: { id },
      data: { retiredAt: null, isActive: true },
    });

    await this.audit.log({
      actorId: ctx.actorId,
      action: 'USER_UNRETIRE',
      targetType: 'user',
      targetId: id,
      ip: ctx.ip,
      userAgent: ctx.userAgent,
    });

    return toUserListItem(updated);
  }
```

- [ ] **Step 4: 시험이 통과하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
pnpm -r typecheck
```

기대: 전부 PASS.

- [ ] **Step 5: 커밋**

메시지:

```
feat(api): 퇴사 처리와 복직을 더한다

퇴사는 retired_at 을 채우면서 is_active 를 함께 내리고 그 계정의 세션을 모두 끊는다.
두 컬럼을 함께 바꾸는 것이 이 설계의 핵심이다. isActive 를 보고 사람을 걸러내는 자리가
서버에 열다섯 군데 있어, 함께 내리면 그 코드가 그대로 퇴사자를 막는다.

자기 자신은 퇴사시킬 수 없고, 활성 ADMIN 이 자기 혼자인 계정도 막는다. 후자는
update() 의 비활성화 금지와 같은 규칙이다.

복직은 항상 활성으로 되돌린다. 퇴사 직전 상태는 USER_RETIRE 감사로그의 wasActive 에
남으므로, 원래 비활성이던 사람은 관리자가 다시 내리면 된다.
```

---

## Task 4: 완전 삭제

**Files:**

- Modify: `apps/api/src/users/users.service.ts`
- Modify: `apps/api/src/users/users.service.test.ts`

**Interfaces:**

- Consumes: Task 2 의 `countActivity`
- Produces: `UsersService.remove(id: string, ctx: ActorContext): Promise<void>`

- [ ] **Step 1: 실패하는 시험을 더한다**

```ts
describe('UsersService.remove()', () => {
  it('활동이 없으면 감사로그의 행위자를 비운 뒤 지운다', async () => {
    const { service, prismaObject, audit, deleted } = buildService({
      users: [userRow({ id: 'u1', username: 'hong', displayName: '홍길동' })],
    });

    await service.remove('u1', CTX);

    expect(prismaObject.auditLog.updateMany).toHaveBeenCalledWith({
      where: { actorId: 'u1' },
      data: { actorId: null },
    });
    expect(deleted).toEqual(['u1']);
    expect(audit.log).toHaveBeenCalledWith(
      expect.objectContaining({
        action: 'USER_DELETE',
        payload: { username: 'hong', displayName: '홍길동' },
      }),
    );
  });

  it('활동이 한 건이라도 있으면 거부한다', async () => {
    const { service, deleted } = buildService({ counts: { comments: 1 } });
    await expect(service.remove('u1', CTX)).rejects.toMatchObject({
      response: { error: 'USER_HAS_ACTIVITY' },
    });
    expect(deleted).toEqual([]);
  });

  it('남을 추가한 기록만 있어도 거부한다', async () => {
    const { service, deleted } = buildService({ counts: { membershipsAdded: 1 } });
    await expect(service.remove('u1', CTX)).rejects.toMatchObject({
      response: { error: 'USER_HAS_ACTIVITY' },
    });
    expect(deleted).toEqual([]);
  });

  it('지우기 직전에 집계를 다시 센다', async () => {
    const { service, prismaObject } = buildService();
    await service.remove('u1', CTX);
    // 트랜잭션 안에서 아홉 번 센다 (projectMember 2, userGroupMember 2, scheduleNode 2, 나머지 3)
    expect(prismaObject.projectMember.count).toHaveBeenCalledTimes(2);
    expect(prismaObject.scheduleNode.count).toHaveBeenCalledTimes(2);
  });

  it('자기 자신은 지울 수 없다', async () => {
    const { service, deleted } = buildService({
      users: [userRow({ id: 'admin1', globalRole: 'ADMIN' })],
    });
    await expect(service.remove('admin1', CTX)).rejects.toMatchObject({
      response: { error: 'SELF_ACTION_FORBIDDEN' },
    });
    expect(deleted).toEqual([]);
  });

  it('퇴사한 사람도 활동이 없으면 지울 수 있다', async () => {
    const { service, deleted } = buildService({
      users: [userRow({ id: 'u1', isActive: false, retiredAt: T0 })],
    });
    await service.remove('u1', CTX);
    expect(deleted).toEqual(['u1']);
  });
});
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
```

기대: FAIL. `service.remove is not a function`.

- [ ] **Step 3: 메서드를 쓴다**

`unretire()` 다음에 더한다.

```ts
  /**
   * 완전 삭제. 활동이 한 건도 없는 계정만 지운다.
   *
   * 감사로그는 **행을 남긴 채 행위자만 비운다.** 계정을 만들어 한 번 로그인하기만 해도
   * LOGIN_SUCCESS 가 남으므로, 그것을 활동으로 세면 지울 수 있는 계정이 사실상 없어져
   * "잘못 만든 계정을 정리한다"는 요청 취지를 절반만 채우게 된다(설계 문서 §3).
   * 세션은 users 행을 지우면 Cascade 로 함께 사라진다.
   *
   * 감사로그를 트랜잭션 밖에서 남기는 이유는, 삭제가 실패했는데 기록만 남는 일을 막기
   * 위함이다. 트랜잭션이 커밋된 뒤에만 기록된다.
   */
  async remove(id: string, ctx: ActorContext): Promise<void> {
    const target = await this.prisma.user.findUnique({ where: { id } });
    if (!target) throw new NotFoundException({ error: 'USER_NOT_FOUND' });
    if (id === ctx.actorId) {
      throw new BadRequestException({ error: 'SELF_ACTION_FORBIDDEN' });
    }

    await this.prisma.$transaction(async (tx) => {
      // 화면이 집계를 읽은 뒤 삭제를 누르기까지 사이에 그 사람이 프로젝트에 추가될 수 있다.
      // 그래서 지우기 직전에 같은 기준으로 한 번 더 센다.
      const { total } = await this.countActivity(id, tx);
      if (total > 0) {
        throw new BadRequestException({ error: 'USER_HAS_ACTIVITY' });
      }

      await tx.auditLog.updateMany({
        where: { actorId: id },
        data: { actorId: null },
      });
      await tx.user.delete({ where: { id } });
    });

    await this.audit.log({
      actorId: ctx.actorId,
      action: 'USER_DELETE',
      targetType: 'user',
      targetId: id,
      ip: ctx.ip,
      userAgent: ctx.userAgent,
      payload: { username: target.username, displayName: target.displayName },
    });
  }
```

- [ ] **Step 4: 시험이 통과하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
pnpm -r typecheck
```

기대: 전부 PASS.

- [ ] **Step 5: 커밋**

메시지:

```
feat(api): 활동이 없는 계정의 완전 삭제를 더한다

지우기 직전에 트랜잭션 안에서 집계를 다시 센다. 화면이 집계를 읽은 뒤 삭제를
누르기까지 사이에 그 사람이 프로젝트에 추가될 수 있기 때문이다.

감사로그는 행을 남긴 채 actor_id 만 비운다. 계정을 만들어 한 번 로그인하기만 해도
LOGIN_SUCCESS 가 남으므로, 그것을 활동으로 세면 지울 수 있는 계정이 사실상 없어진다.
"2026-09-01 10:00 에 로그인이 있었다"는 사실은 남고 누구였는지만 사라진다.

삭제 사실 자체는 관리자를 행위자로 하는 USER_DELETE 로 따로 남으므로 추적이 끊기지
않는다. 그 기록은 트랜잭션이 커밋된 뒤에만 남긴다.
```

---

## Task 5: 목록의 퇴사자 필터와 라우트 넷

**Files:**

- Modify: `apps/api/src/users/users.service.ts`
- Modify: `apps/api/src/users/users.controller.ts`
- Modify: `apps/api/src/users/users.service.test.ts`

**Interfaces:**

- Consumes: Task 2·3·4 의 서비스 메서드
- Produces: `GET /admin/users?includeRetired=1`, `GET /admin/users/:id/activity`, `POST /admin/users/:id/retire`, `POST /admin/users/:id/unretire`, `DELETE /admin/users/:id`

- [ ] **Step 1: 실패하는 시험을 더한다**

`buildService` 의 `user.findMany` 대역이 지금은 조건을 무시하고 전부 돌려준다. `where` 를 반영하도록 고친다.

```ts
      findMany: vi.fn(async ({ where }: { where?: { retiredAt?: null; isActive?: boolean } }) =>
        users.filter(
          (u) =>
            (where?.retiredAt === undefined || u.retiredAt === null) &&
            (where?.isActive === undefined || u.isActive === where.isActive),
        ),
      ),
```

이어서 시험을 더한다.

```ts
describe('UsersService.list()', () => {
  const seed = {
    users: [
      userRow({ id: 'active1' }),
      userRow({ id: 'inactive1', isActive: false }),
      userRow({ id: 'retired1', isActive: false, retiredAt: T0 }),
    ],
  };

  it('기본으로는 퇴사자를 빼고 보여준다', async () => {
    const { service } = buildService(seed);
    const rows = await service.list({ status: 'all' });
    expect(rows.map((r) => r.id)).toEqual(['active1', 'inactive1']);
  });

  it('includeRetired 를 켜면 퇴사자도 보여준다', async () => {
    const { service } = buildService(seed);
    const rows = await service.list({ status: 'all', includeRetired: true });
    expect(rows.map((r) => r.id)).toEqual(['active1', 'inactive1', 'retired1']);
  });

  it('퇴사 시각을 응답에 담는다', async () => {
    const { service } = buildService(seed);
    const rows = await service.list({ status: 'all', includeRetired: true });
    expect(rows.find((r) => r.id === 'retired1')!.retiredAt).toBe(T0.toISOString());
    expect(rows.find((r) => r.id === 'active1')!.retiredAt).toBeNull();
  });
});
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
```

기대: 첫 시험이 FAIL. 퇴사자까지 셋이 나온다.

- [ ] **Step 3: `list` 에 필터를 더한다**

`users.service.ts` 의 `list` 시그니처와 `where` 구성을 고친다.

```ts
  async list(args: {
    query?: string | undefined;
    status?: 'active' | 'inactive' | 'all' | undefined;
    includeRetired?: boolean | undefined;
  }): Promise<UserListItem[]> {
    const where: Record<string, unknown> = {};
    if (args.status === 'active') where.isActive = true;
    else if (args.status === 'inactive') where.isActive = false;
    // 재직 여부와 활성 여부는 독립한 축이다. 기본 화면에서는 퇴사자를 감춘다(확정명세 §6-나).
    if (!args.includeRetired) where.retiredAt = null;
    if (args.query && args.query.length > 0) {
```

이후 본문은 그대로 두고, 반환문이 Task 2 에서 `return users.map(toUserListItem);` 으로 바뀌어 있는지 확인한다.

- [ ] **Step 4: 시험이 통과하는지 확인한다**

```bash
pnpm -F @sam/api test -- users.service.test.ts
```

기대: 전부 PASS.

- [ ] **Step 5: 컨트롤러에 라우트를 붙인다**

`users.controller.ts` 의 임포트에 `Delete` 를 더하고, `UserActivitySummary` 타입을 가져온다.

```ts
import {
  Body,
  Controller,
  Delete,
  Get,
  HttpCode,
  Param,
  Patch,
  Post,
  Query,
  Req,
  UseGuards,
  UsePipes,
} from '@nestjs/common';
import {
  CreateUserDto,
  UpdateUserDto,
  type ResetPasswordResponse,
  type UserActivitySummary,
  type UserListItem,
} from '@sam/shared';
```

`list` 를 고친다.

```ts
  @Get()
  list(
    @Query('query') query?: string,
    @Query('status') status?: 'active' | 'inactive' | 'all',
    @Query('includeRetired') includeRetired?: string,
  ): Promise<UserListItem[]> {
    // 쿼리 파라미터는 항상 문자열이다. '1' 일 때만 참으로 본다.
    return this.users.list({ query, status, includeRetired: includeRetired === '1' });
  }
```

`unlock` 다음에 라우트 넷을 더한다.

```ts
  @Get(':id/activity')
  activity(@Param('id') id: string): Promise<UserActivitySummary> {
    return this.users.activity(id);
  }

  @Post(':id/retire')
  @HttpCode(200)
  retire(
    @Param('id') id: string,
    @Req() req: AuthenticatedRequest,
  ): Promise<UserListItem> {
    return this.users.retire(id, {
      actorId: req.user!.id,
      ip: getClientIp(req),
      userAgent: getUserAgent(req),
    });
  }

  @Post(':id/unretire')
  @HttpCode(200)
  unretire(
    @Param('id') id: string,
    @Req() req: AuthenticatedRequest,
  ): Promise<UserListItem> {
    return this.users.unretire(id, {
      actorId: req.user!.id,
      ip: getClientIp(req),
      userAgent: getUserAgent(req),
    });
  }

  @Delete(':id')
  @HttpCode(204)
  async remove(
    @Param('id') id: string,
    @Req() req: AuthenticatedRequest,
  ): Promise<void> {
    await this.users.remove(id, {
      actorId: req.user!.id,
      ip: getClientIp(req),
      userAgent: getUserAgent(req),
    });
  }
```

**클래스에 이미 `@UseGuards(OriginGuard)` 와 `@AdminOnly()` 가 붙어 있으므로 메서드마다 다시 붙이지 않는다.**

- [ ] **Step 6: 전체 검사**

```bash
pnpm -r typecheck
pnpm -r test
```

기대: 전부 통과.

- [ ] **Step 7: 커밋**

메시지:

```
feat(api): 퇴사자 필터와 라우트 넷을 붙인다

목록은 기본으로 퇴사자를 감추고 includeRetired=1 일 때만 함께 보여준다. 기존 status
(활성·비활성)와 별개의 파라미터로 둔 것은 재직 여부와 활성 여부가 독립한 축이기
때문이다.

라우트는 활동 집계 조회, 퇴사, 복직, 삭제 넷이다. UsersController 에 클래스 단위로
OriginGuard 와 AdminOnly 가 이미 붙어 있어 그대로 상속받는다.
```

---

## Task 6: 웹 훅과 사용자 관리 목록

**Files:**

- Modify: `apps/web/src/lib/users.ts`
- Modify: `apps/web/src/pages/AdminUsersPage.tsx`

**Interfaces:**

- Consumes: Task 5 의 라우트 넷
- Produces:
  - `useUsers(opts?: { query?: string; status?: UserListStatus; includeRetired?: boolean })`
  - `usersKey({ query, status, includeRetired })`
  - `useUserActivity(id: string)` — `UseQueryResult<UserActivitySummary>`
  - `useRetireUser()`, `useUnretireUser()`, `useDeleteUser()` — 모두 `mutateAsync(id: string)`

- [ ] **Step 1: 훅을 더한다**

`apps/web/src/lib/users.ts` 를 고친다. 임포트에 `UserActivitySummary` 를 더한다.

```ts
import type {
  CreateUserDto,
  ResetPasswordResponse,
  UpdateUserDto,
  UserActivitySummary,
  UserListItem,
} from '@sam/shared';
```

키와 목록 훅을 고친다.

```ts
export const usersKey = (opts: {
  query: string;
  status: UserListStatus;
  includeRetired: boolean;
}) => ['admin', 'users', opts] as const;

export function useUsers(opts?: {
  query?: string;
  status?: UserListStatus;
  includeRetired?: boolean;
}) {
  const query = opts?.query ?? '';
  const status: UserListStatus = opts?.status ?? 'active';
  const includeRetired = opts?.includeRetired ?? false;
  return useQuery<UserListItem[]>({
    queryKey: usersKey({ query, status, includeRetired }),
    queryFn: () => {
      const params = new URLSearchParams();
      if (query) params.set('query', query);
      params.set('status', status);
      if (includeRetired) params.set('includeRetired', '1');
      const qs = params.toString();
      return api.get<UserListItem[]>(`/admin/users${qs ? `?${qs}` : ''}`);
    },
  });
}

export const userActivityKey = (id: string) => ['admin', 'users', id, 'activity'] as const;

/**
 * 계정 하나의 활동 집계. 삭제 버튼을 띄울지 판단하는 근거다.
 *
 * 프로젝트 참여나 그룹 소속을 이 화면에서 빼면 집계가 달라지므로, 그 조작들이
 * ['admin', 'users'] 를 무효화할 때 이 쿼리도 함께 다시 읽힌다.
 */
export function useUserActivity(id: string) {
  return useQuery<UserActivitySummary>({
    queryKey: userActivityKey(id),
    queryFn: () => api.get<UserActivitySummary>(`/admin/users/${id}/activity`),
  });
}
```

`useUnlockUser` 다음에 뮤테이션 셋을 더한다.

```ts
export function useRetireUser() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (id: string) =>
      api.post<UserListItem>(`/admin/users/${id}/retire`, {}),
    onSuccess: () => invalidateAll(qc),
  });
}

export function useUnretireUser() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (id: string) =>
      api.post<UserListItem>(`/admin/users/${id}/unretire`, {}),
    onSuccess: () => invalidateAll(qc),
  });
}

export function useDeleteUser() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => api.delete<void>(`/admin/users/${id}`),
    onSuccess: () => invalidateAll(qc),
  });
}
```

`invalidateAll` 이 `['admin', 'users']` 를 무효화하므로 `userActivityKey` 도 함께 걸린다(접두어가 같다).

- [ ] **Step 2: 목록 화면에 체크상자를 단다**

`AdminUsersPage.tsx` 의 상태 선언(23행 부근)에 하나를 더한다.

```ts
  const [status, setStatus] = useState<UserListStatus>('all');
  const [includeRetired, setIncludeRetired] = useState(false);
```

조회를 고친다.

```ts
  const users = useUsers({ status, includeRetired });
```

상태 드롭다운을 감싼 `<label>` 다음에 체크상자를 더한다.

```tsx
        <label className="flex items-center gap-2 text-sm">
          <input
            type="checkbox"
            checked={includeRetired}
            onChange={(e) => setIncludeRetired(e.target.checked)}
            className="h-4 w-4 rounded border-slate-300 dark:border-slate-700"
          />
          <span className="text-slate-600 dark:text-slate-400">퇴사자 포함</span>
        </label>
```

- [ ] **Step 3: 퇴사 배지를 붙이고 토글을 감춘다**

`UserRow` 안에서 판정 값을 하나 만든다. `isLocked` 선언 다음에 넣는다.

```ts
  const isRetired = user.retiredAt !== null;
```

"비활성" 배지 다음에 퇴사 배지를 더한다. **비활성 배지는 그대로 둔다.** 퇴사자는 반드시 비활성이므로 배지 둘이 함께 뜨는 것이 맞다.

```tsx
          {isRetired && (
            <span className="rounded border border-purple-300 bg-purple-50 px-1.5 py-0.5 text-[10px] font-semibold text-purple-700 dark:border-purple-700 dark:bg-purple-950 dark:text-purple-200">
              퇴사
            </span>
          )}
```

활성 토글 버튼을 조건으로 감싼다. 퇴사자를 활성으로 되돌리면 "퇴사했는데 로그인은 되는" 조합이 만들어지므로, 상태를 바꾸는 길은 상세 화면의 복직 하나로 모은다.

```tsx
        {!isRetired && (
          <button
            type="button"
            onClick={onToggleActive}
            disabled={busy || isSelf}
            title={isSelf ? '자기 자신은 토글할 수 없습니다.' : undefined}
            className={`rounded border px-2 py-1 text-xs font-semibold disabled:opacity-50 ${
              user.isActive
                ? 'border-amber-300 text-amber-800 hover:bg-amber-50 dark:border-amber-700 dark:text-amber-200 dark:hover:bg-amber-950'
                : 'border-emerald-300 text-emerald-700 hover:bg-emerald-50 dark:border-emerald-700 dark:text-emerald-300 dark:hover:bg-emerald-950'
            }`}
          >
            {user.isActive ? '비활성화' : '활성화'}
          </button>
        )}
```

- [ ] **Step 4: 타입 검사와 번들 빌드**

```bash
pnpm -r typecheck
pnpm -F @sam/web build
```

기대: 둘 다 통과.

- [ ] **Step 5: 커밋**

메시지:

```
feat(web): 사용자 관리 목록에 퇴사자 보기와 배지를 더한다

기본 화면에서는 퇴사자가 보이지 않고 "퇴사자 포함" 을 켜야 나타난다.

퇴사자 행에서는 활성 토글 버튼을 감춘다. 그것을 눌러 활성으로 되돌리면 퇴사했는데
로그인은 되는 조합이 만들어지기 때문이다. 상태를 바꾸는 길은 상세 화면의 복직
하나로 모은다.

비활성 배지는 그대로 두고 퇴사 배지를 함께 띄운다. 퇴사자는 반드시 비활성이므로
둘이 같이 뜨는 것이 상태를 정확히 나타낸다.
```

---

## Task 7: 사용자 상세의 계정 정리 섹션

**Files:**

- Create: `apps/web/src/components/UserDeleteConfirmDialog.tsx`
- Modify: `apps/web/src/pages/AdminUserDetailPage.tsx`

**Interfaces:**

- Consumes: Task 6 의 `useUserActivity`·`useRetireUser`·`useUnretireUser`·`useDeleteUser`
- Produces: `AccountCleanupSection({ user }: { user: UserListItem })` — `AdminUserDetailPage` 안의 지역 컴포넌트

- [ ] **Step 1: 삭제 확인 대화상자를 만든다**

`apps/web/src/components/UserDeleteConfirmDialog.tsx` 를 새로 만든다.

```tsx
import { useState } from 'react';

/**
 * 계정 완전 삭제 확인.
 *
 * **아이디를 직접 입력해야 버튼이 열린다.** 되돌릴 수 없는 작업이라 window.confirm 한 번으로
 * 넘기지 않는다. 활성·비활성 토글이나 비밀번호 리셋과 달리 이것은 복구할 방법이 없다.
 */
export default function UserDeleteConfirmDialog({
  username,
  displayName,
  busy,
  onConfirm,
  onClose,
}: {
  username: string;
  displayName: string;
  busy: boolean;
  onConfirm: () => void;
  onClose: () => void;
}) {
  const [typed, setTyped] = useState('');
  const matched = typed.trim() === username;

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4">
      <div className="w-full max-w-md rounded-lg bg-white p-5 shadow-xl dark:bg-slate-900">
        <h2 className="text-base font-semibold text-rose-700 dark:text-rose-300">
          계정을 영구히 삭제합니다
        </h2>

        <p className="mt-3 text-sm font-medium">
          {username} <span className="text-slate-500">({displayName})</span>
        </p>

        <p className="mt-3 text-sm text-slate-600 dark:text-slate-400">
          이 작업은 되돌릴 수 없습니다. 계정과 로그인 세션이 사라지며, 감사 기록에는 행위자 없이
          남습니다.
        </p>

        <label className="mt-4 block text-sm">
          <span className="text-slate-600 dark:text-slate-400">
            계속하려면 아이디를 입력하십시오
          </span>
          <input
            type="text"
            value={typed}
            onChange={(e) => setTyped(e.target.value)}
            autoComplete="off"
            className="mt-1 w-full rounded border border-slate-300 bg-white px-3 py-1.5 text-sm dark:border-slate-700 dark:bg-slate-950"
          />
        </label>

        <div className="mt-5 flex justify-end gap-2">
          <button
            type="button"
            onClick={onClose}
            disabled={busy}
            className="rounded border border-slate-300 px-3 py-1.5 text-sm font-semibold disabled:opacity-50 dark:border-slate-700"
          >
            취소
          </button>
          <button
            type="button"
            onClick={onConfirm}
            disabled={!matched || busy}
            className="rounded bg-rose-600 px-3 py-1.5 text-sm font-semibold text-white hover:bg-rose-700 disabled:opacity-40"
          >
            삭제
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 상세 화면에 임포트를 더한다**

`AdminUserDetailPage.tsx` 의 임포트를 고친다.

```ts
import { useNavigate } from 'react-router-dom';
import {
  useUsers,
  useUpdateUser,
  useUserActivity,
  useRetireUser,
  useUnretireUser,
  useDeleteUser,
} from '../lib/users';
import UserDeleteConfirmDialog from '../components/UserDeleteConfirmDialog';
```

`useNavigate` 는 기존 `react-router-dom` 임포트 줄(`Link, Navigate, useParams`)에 합쳐서 넣는다.

- [ ] **Step 3: 계정 정리 섹션을 쓴다**

`AdminUserDetailPage.tsx` 의 `AddProjectsDialog` 앞에 컴포넌트를 더한다.

```tsx
/**
 * 계정 정리 — 퇴사 처리·복직과 완전 삭제.
 *
 * 삭제 버튼은 **활동이 한 건이라도 있으면 아예 보이지 않는다**(확정명세 §6-가). 대신 무엇이
 * 남아 있어 지울 수 없는지 적는다. 집계를 "정리하면 없어지는 것"과 "지울 수 없는 것"으로 나눈
 * 덕분에, 앞의 것만 남은 계정에는 위 섹션에서 빼면 지울 수 있다고 안내할 수 있다.
 */
function AccountCleanupSection({ user }: { user: UserListItem }) {
  const navigate = useNavigate();
  const activity = useUserActivity(user.id);
  const retire = useRetireUser();
  const unretire = useUnretireUser();
  const remove = useDeleteUser();
  const [deleteOpen, setDeleteOpen] = useState(false);

  const busy = retire.isPending || unretire.isPending || remove.isPending;
  const isRetired = user.retiredAt !== null;

  async function onRetire() {
    const ok = window.confirm(
      `${user.displayName} 님을 퇴사 처리하시겠습니까?\n\n` +
        '· 로그인이 막히고 지금 접속 중인 세션이 즉시 끊어집니다\n' +
        '· 사용자 목록 기본 화면에서 감춰집니다\n' +
        '· 프로젝트 참여자와 그룹 인원 후보에서 빠집니다\n' +
        '· 소속 그룹과 참여 중인 프로젝트는 그대로 남습니다\n' +
        '· 나중에 복직 처리로 되돌릴 수 있습니다',
    );
    if (!ok) return;
    try {
      await retire.mutateAsync(user.id);
      toast.success('퇴사 처리되었습니다.');
    } catch (err) {
      toast.error(apiErrorMessage(err));
    }
  }

  async function onUnretire() {
    try {
      await unretire.mutateAsync(user.id);
      toast.success('복직 처리되었습니다. 다시 로그인할 수 있습니다.');
    } catch (err) {
      toast.error(apiErrorMessage(err));
    }
  }

  async function onDelete() {
    try {
      await remove.mutateAsync(user.id);
      setDeleteOpen(false);
      toast.success('계정이 삭제되었습니다.');
      // 방금 지운 계정의 상세에 머무르면 다음 조회가 USER_NOT_FOUND 로 실패한다.
      navigate('/admin/users');
    } catch (err) {
      toast.error(apiErrorMessage(err));
    }
  }

  return (
    <section className="mt-4 rounded-lg border border-slate-200 p-4 dark:border-slate-700">
      <h2 className="text-sm font-semibold">계정 정리</h2>

      {isRetired && (
        <p className="mt-2 text-sm text-slate-600 dark:text-slate-400">
          {formatDay(user.retiredAt!)} 에 퇴사 처리되었습니다. 로그인이 막혀 있고 목록 기본
          화면에서 감춰집니다. 소속 그룹과 참여 프로젝트는 그대로 남아 있습니다.
        </p>
      )}

      <div className="mt-3 text-sm">
        <h3 className="text-xs font-semibold text-slate-500">활동 기록</h3>
        {activity.isLoading && <p className="mt-1 text-slate-500">세는 중…</p>}
        {activity.isError && (
          <p className="mt-1 text-rose-600">{apiErrorMessage(activity.error)}</p>
        )}
        {activity.data && <ActivityLines summary={activity.data} />}
      </div>

      <div className="mt-4 flex flex-wrap gap-2">
        {isRetired ? (
          <button
            type="button"
            onClick={onUnretire}
            disabled={busy}
            className="rounded border border-emerald-300 px-3 py-1.5 text-sm font-semibold text-emerald-700 hover:bg-emerald-50 disabled:opacity-50 dark:border-emerald-700 dark:text-emerald-300 dark:hover:bg-emerald-950"
          >
            복직 처리
          </button>
        ) : (
          <button
            type="button"
            onClick={onRetire}
            disabled={busy}
            className="rounded border border-amber-300 px-3 py-1.5 text-sm font-semibold text-amber-800 hover:bg-amber-50 disabled:opacity-50 dark:border-amber-700 dark:text-amber-200 dark:hover:bg-amber-950"
          >
            퇴사 처리
          </button>
        )}

        {activity.data?.canDelete === true && (
          <button
            type="button"
            onClick={() => setDeleteOpen(true)}
            disabled={busy}
            className="rounded border border-rose-300 px-3 py-1.5 text-sm font-semibold text-rose-700 hover:bg-rose-50 disabled:opacity-50 dark:border-rose-700 dark:text-rose-300 dark:hover:bg-rose-950"
          >
            계정 삭제
          </button>
        )}
      </div>

      {deleteOpen && (
        <UserDeleteConfirmDialog
          username={user.username}
          displayName={user.displayName}
          busy={remove.isPending}
          onConfirm={onDelete}
          onClose={() => setDeleteOpen(false)}
        />
      )}
    </section>
  );
}

/**
 * 집계를 사람이 읽는 문장으로 바꾼다.
 *
 * 두 갈래를 나눠 보여주는 이유는 관리자가 무엇을 하면 지울 수 있는지 알려 주기 위함이다.
 * 지울 수 없는 것이 하나라도 있으면 정리해도 소용없으므로 그 사실을 먼저 말한다.
 */
function ActivityLines({ summary }: { summary: UserActivitySummary }) {
  const { clearable, permanent } = summary;

  const clearableParts: string[] = [];
  if (clearable.projectMemberships > 0)
    clearableParts.push(`참여 프로젝트 ${clearable.projectMemberships}건`);
  if (clearable.groupMemberships > 0)
    clearableParts.push(`그룹 소속 ${clearable.groupMemberships}건`);

  const permanentParts: string[] = [];
  if (permanent.createdProjects > 0)
    permanentParts.push(`프로젝트 ${permanent.createdProjects}건 생성`);
  if (permanent.nodesCreated > 0) permanentParts.push(`일정 ${permanent.nodesCreated}건 생성`);
  if (permanent.nodesUpdated > 0) permanentParts.push(`일정 ${permanent.nodesUpdated}건 수정`);
  if (permanent.comments > 0) permanentParts.push(`댓글 ${permanent.comments}건`);
  if (permanent.history > 0) permanentParts.push(`변경 이력 ${permanent.history}건`);
  if (permanent.membershipsAdded > 0)
    permanentParts.push(`남을 프로젝트에 넣은 기록 ${permanent.membershipsAdded}건`);
  if (permanent.groupMembersAdded > 0)
    permanentParts.push(`남을 그룹에 넣은 기록 ${permanent.groupMembersAdded}건`);

  if (summary.canDelete) {
    return <p className="mt-1 text-slate-600 dark:text-slate-400">없습니다.</p>;
  }

  return (
    <div className="mt-1 space-y-1">
      {clearableParts.length > 0 && (
        <p className="text-slate-600 dark:text-slate-400">{clearableParts.join(' · ')}</p>
      )}
      {permanentParts.length > 0 && (
        <p className="text-slate-600 dark:text-slate-400">{permanentParts.join(' · ')}</p>
      )}
      <p className="pt-1 text-[13px] text-slate-500">
        {permanentParts.length > 0
          ? `이 계정은 ${permanentParts.join(', ')} 기록이 있어 삭제할 수 없습니다. 퇴사 처리를 이용하십시오.`
          : '위 섹션에서 참여와 소속을 정리하면 이 계정을 삭제할 수 있습니다.'}
      </p>
    </div>
  );
}

/** 퇴사 시각을 날짜까지만 보여준다. */
function formatDay(iso: string): string {
  const d = new Date(iso);
  const pad = (n: number) => n.toString().padStart(2, '0');
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
}
```

`UserActivitySummary` 타입을 파일 상단 `@sam/shared` 임포트 목록에 더한다.

- [ ] **Step 4: 섹션을 화면에 끼운다**

`AdminUserDetailPage` 본문에서 `ProjectSection` 을 렌더링하는 자리 바로 다음에 넣는다. **참여 프로젝트 섹션 아래가 맞는 자리다** — 정리하면 없어지는 것 두 갈래를 다루는 섹션이 바로 위에 있어야 관리자가 안내를 읽고 곧바로 손볼 수 있다.

88행 부근이 지금 이렇게 되어 있다.

```tsx
          <AccountSection user={user} />
          <GroupSection userId={id} displayName={user.displayName} />
          <ProjectSection userId={id} />
```

여기에 한 줄을 더한다.

```tsx
          <AccountSection user={user} />
          <GroupSection userId={id} displayName={user.displayName} />
          <ProjectSection userId={id} />
          <AccountCleanupSection user={user} />
```

- [ ] **Step 5: 타입 검사와 번들 빌드**

```bash
pnpm -r typecheck
pnpm -F @sam/web build
```

기대: 둘 다 통과.

- [ ] **Step 6: 커밋**

메시지:

```
feat(web): 사용자 상세에 계정 정리 섹션을 더한다

퇴사 처리·복직과 완전 삭제를 한 자리에 모은다. 참여 프로젝트 섹션 바로 아래에 둔 것은,
"정리하면 삭제할 수 있다" 는 안내를 읽고 곧바로 위에서 손볼 수 있게 하기 위함이다.

삭제 버튼은 활동이 한 건이라도 있으면 아예 보이지 않고, 무엇이 남아 있어 지울 수
없는지 적는다. 집계를 두 갈래로 나눈 덕분에 "빼면 지울 수 있다" 와 "영구히 지울 수
없다" 를 구별해 안내한다.

삭제 확인은 아이디를 직접 입력하게 한다. 되돌릴 수 없는 작업이라 확인 창 한 번으로
넘기지 않는다.
```

---

## Task 8: 화면 직접 확인

**Files:** 없음 (확인만 한다. 문제가 드러나면 그때 고칠 파일을 정한다)

**Interfaces:**

- Consumes: Task 1~7 의 모든 변경
- Produces: 확인 결과 보고

**환경 주의:** 개발 서버가 이미 떠 있을 수 있다. **사용자의 것이므로 끄지 말고 그대로 쓴다.** 브라우저로는 5173 에 접속한다. 3000 을 직접 열면 옛 exe 산출물이 뜬다. 새 관리 화면은 **관리자 모드를 켜야** 열린다(헤더의 방패 표시).

**시험 데이터 방침:** 이번에는 **시험용 계정을 만들어 실제로 지워 볼 수 있다.** 2차의 화면 확인에서는 지울 방법이 없어 "새 사용자를 만들지 말 것"이 방침이었는데, 이 작업이 바로 그 방법을 만든다. 계정 이름에 `검증-` 접두어를 붙인다.

- [ ] **Step 1: 삭제 경로를 확인한다**

| # | 조작 | 기대 |
| --- | --- | --- |
| 1 | `검증-삭제1` 계정을 만들고 상세로 가서 삭제 | 활동 기록이 "없습니다" 로 나오고 삭제 버튼이 보인다. 지우면 목록으로 돌아가고 그 계정이 사라진다 |
| 2 | `검증-삭제2` 를 만들어 **그 계정으로 한 번 로그인**한 뒤 관리자로 돌아와 삭제 | **감사로그가 있어도 지워진다** (설계 §3 의 결정이 실제로 동작하는가) |
| 3 | 2번 직후 감사로그를 확인 | 그 로그인 기록의 행이 남아 있고 행위자만 비어 있다 |

3 번은 화면에 감사로그 조회가 없으면 다음으로 확인한다.

```bash
sqlite3 <DATABASE_URL 이 가리키는 파일> \
  "SELECT action, actor_id FROM audit_logs WHERE action='LOGIN_SUCCESS' ORDER BY occurred_at DESC LIMIT 5;"
```

기대: 방금 지운 계정의 행이 `actor_id` 가 비어 있는 채로 보인다.

- [ ] **Step 2: 삭제 거부를 확인한다**

| # | 조작 | 기대 |
| --- | --- | --- |
| 4 | `검증-삭제3` 을 만들어 프로젝트에 참여자로 넣기 | 삭제 버튼이 사라지고 "위 섹션에서 참여와 소속을 정리하면 삭제할 수 있습니다" 가 뜬다 |
| 5 | 그 프로젝트에서 빼기 | 삭제 버튼이 **다시 나타난다** |
| 6 | 그 계정을 그룹에 넣기 | 다시 사라지고 "그룹 소속 1건" 이 나온다 |
| 7 | 그룹에서 빼고, 그 계정으로 일정을 하나 만들게 하기 | "일정 1건 생성 … 기록이 있어 삭제할 수 없습니다. 퇴사 처리를 이용하십시오" 로 문구가 바뀐다 |
| 8 | 자기 자신(관리자)의 상세에서 삭제·퇴사 시도 | 거부된다 |

8 번에서 삭제 버튼 자체가 안 보일 수 있다(관리자는 남을 추가한 기록이 있어 `canDelete` 가 거짓이다). 그 경우 퇴사 버튼만 눌러 `SELF_ACTION_FORBIDDEN` 문구가 뜨는지 본다.

- [ ] **Step 3: 퇴사와 복직을 확인한다**

| # | 조작 | 기대 |
| --- | --- | --- |
| 9 | 7 번 계정을 퇴사 처리 | 확인 창에 다섯 줄 안내가 뜬다. 승인하면 목록 기본 화면에서 사라진다 |
| 10 | "퇴사자 포함" 을 켜기 | 그 계정이 **퇴사·비활성 배지 둘을 달고** 나타난다 |
| 11 | 그 행의 활성 토글 버튼 | **보이지 않는다** |
| 12 | 퇴사자로 로그인 시도 | 막힌다 |
| 13 | 퇴사 직전에 그 계정으로 다른 브라우저에서 접속해 두었다면 | 세션이 끊겨 로그인 화면으로 돌아간다 |
| 14 | 프로젝트 참여자 추가 후보와 그룹 인원 추가 후보 | 퇴사자가 **나오지 않는다** |
| 15 | 퇴사자 상세의 소속 그룹과 참여 프로젝트 | **그대로 남아 있다** |
| 16 | 복직 처리 | 활성으로 돌아오고 14 번 후보 목록에 다시 나타난다 |

- [ ] **Step 4: 회귀를 확인한다**

| # | 확인할 것 | 기대 |
| --- | --- | --- |
| 17 | 상태 드롭다운(전체·활성·비활성) | 예전처럼 동작하고, 퇴사자는 "퇴사자 포함" 을 켜야만 섞여 나온다 |
| 18 | 재직자의 활성·비활성 토글 | 예전처럼 동작한다 |
| 19 | 비밀번호 리셋·잠금 해제 | 예전처럼 동작한다 |
| 20 | 화면이 빈 채로 뜨는 곳 | 없다. 브라우저 콘솔에 오류가 없다 |

- [ ] **Step 5: 정리하고 보고한다**

만든 `검증-` 계정을 지우거나 퇴사 처리하고, 넣었던 프로젝트·그룹에서 뺀다. 일정을 만든 계정은 지울 수 없으므로 퇴사 처리해 둔다. **되돌리지 못한 것을 보고서에 정확히 적는다.**

20개 항목을 번호대로 모두 적고 각각 통과·실패·확인 불가 중 하나로 판정한다. **통과했다고 적으려고 절차를 건너뛰지 말 것.** 확인하지 못한 것은 "확인 불가" 로 적고 이유를 쓴다.

**이 과제는 커밋할 것이 없다.**

---

## Self-Review

계획을 다 쓴 뒤 설계 문서와 대조한 결과다.

### 사양 대응표

| 설계 문서 절 | 담당 과제 |
| --- | --- |
| §4 상태 모델 | Task 3 (전이), Task 6 Step 3 (토글 감추기로 금지 조합 방지) |
| §5.1 아홉 갈래 | Task 2 |
| §5.2 둘로 나누어 보여주기 | Task 1 (타입), Task 7 (`ActivityLines`) |
| §5.3 서버가 두 번 판정 | Task 4 |
| §6.1 집계 응답 | Task 1 Step 2 |
| §6.2 `includeRetired` | Task 5 |
| §6.3 거부 규칙 여섯 | Task 3 (넷), Task 4 (둘) |
| §6.4 삭제 실행 순서 | Task 4 Step 3 |
| §6.5 감사 액션 셋 | Task 1 Step 3 |
| §7.1 목록 화면 | Task 6 |
| §7.2 계정 정리 섹션 | Task 7 |
| §7.3 확인 창 둘 | Task 7 (삭제는 전용 대화상자, 퇴사는 `window.confirm`) |
| §8 다른 화면 (고칠 것 없음) | Task 8 항목 14 로 확인만 한다 |
| §9 검증 계획 | 각 과제의 시험 단계와 Task 8 |
| §10 DB 변경 없음 | Global Constraints 에 "schema.prisma 를 건드리지 않는다" 로 못 박았다 |

### 계획을 쓰면서 정한 것

1. **`toUserListItem` 을 Task 2 에서 추출한다.** 설계 문서에는 없다. `UserListItem` 에 `retiredAt` 이 늘면서 응답 매핑 세 곳이 동시에 깨지는데, 여기에 `retire`·`unretire` 두 곳이 더해져 다섯 곳이 된다. 필드가 늘 때마다 다섯 곳을 고치다 한 곳을 빠뜨릴 자리라 한 함수로 모은다. Task 1 의 Step 5 가 일부러 타입 오류를 남긴 채 끝나는 것도 이 때문이다.
2. **`countActivity` 가 `Prisma.TransactionClient` 를 받는다.** 설계 §5.3 이 "삭제 직전에 다시 센다" 고만 적었는데, 그것을 트랜잭션 안에서 하려면 클라이언트를 받아야 한다. `PrismaService` 가 `PrismaClient` 를 확장하므로 트랜잭션 밖에서는 `this.prisma` 를 그대로 넘긴다.
3. **퇴사 확인은 `window.confirm` 으로 한다.** 삭제만 전용 대화상자를 만든다. 되돌릴 수 있는 조작과 없는 조작의 무게를 화면에서도 다르게 두는 것이고, 기존 코드(`AdminUsersPage` 의 토글·비밀번호 리셋)가 이미 같은 방식이다.
4. **비활성 배지와 퇴사 배지를 함께 띄운다.** 설계 §7.1 은 "구별되는 퇴사 배지" 라고만 적었다. 퇴사자는 반드시 비활성이므로 둘이 함께 뜨는 것이 상태를 정확히 나타낸다.

### 확인한 타입 일관성

- `UserActivitySummary.clearable` 은 두 필드, `permanent` 는 일곱 필드다. Task 1 의 Zod 정의, Task 2 의 `countActivity` 반환, Task 7 의 `ActivityLines` 가 같은 이름을 쓴다.
- `countActivity` 는 `{ summary, total }` 을 돌려준다. `activity()` 는 `summary` 만, `remove()` 는 `total` 만 쓴다.
- 뮤테이션 셋(`useRetireUser`·`useUnretireUser`·`useDeleteUser`)은 모두 `mutateAsync(id: string)` 이다. 인자 모양이 같아 호출부에서 헷갈리지 않는다.
- `usersKey` 가 인자 하나를 더 받으므로, 이 키를 쓰는 다른 자리가 있으면 Task 6 의 타입 검사가 잡아 준다.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-09-04-사용자삭제-퇴사처리.md`.**

`master` 에서 바로 작업할지 작업 브랜치를 팔지는 실행 전에 정한다. 2차는 `feat/user-groups-and-member-permissions` 에서 진행한 뒤 머지했다.

**이 작업에는 마이그레이션이 없으므로 fly.io 볼륨 스냅샷을 미리 뜰 필요가 없다.** 다만 3차를 마치면 2차와 함께 하나의 배포로 나가므로, 그때 2차 계획의 Task 13(배포판 예행 연습)을 최신 코드로 한 번 수행해야 한다.
