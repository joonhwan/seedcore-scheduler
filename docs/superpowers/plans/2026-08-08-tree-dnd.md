# 트리 드래그 앤 드롭 (순서·부모 변경) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 프로젝트 상세 화면의 트리에서 마우스 드래그 한 번으로 일정 노드의 순서와 부모 그룹을 바꿀 수 있게 한다.

**Architecture:** 드롭 대상을 `(행 경계 boundary, 깊이 depth)` 한 쌍으로 모델링한다. 세로 위치가 어느 두 행 사이인지를, 가로 위치가 들여쓰기 깊이(= 부모)를 정한다. 계산은 전부 DOM 의존성 없는 순수 함수 `apps/web/src/lib/treeDnd.ts` 에 모으고 vitest 로 검증한다. `Timeline.tsx` 에는 포인터 이벤트 배선과 그리기만 남긴다. 백엔드는 손대지 않는다 — 기존 `POST /nodes/:id/move` 가 부모·순서·깊이·재정렬을 모두 처리한다.

**Tech Stack:** React 18, TypeScript 5.6, Vite 5, Tailwind CSS, vitest 2, TanStack Query 5, Pointer Events API

**설계 문서:** `docs/superpowers/specs/2026-08-08-tree-dnd-reorder-design.md`

## Global Constraints

- **`cd` 금지.** 모든 명령은 저장소 루트에서 `pnpm -F @sam/web ...` 형태로 실행한다.
- **타입 검사 필수.** 코드 수정 후 `pnpm -r typecheck` 로 컴파일 에러가 없는지 확인한다.
- **`exactOptionalPropertyTypes: true`** 가 켜져 있다. optional 프로퍼티에 `undefined` 를 명시적으로 넣을 수 없다. 새 prop 타입은 기존 `Timeline.tsx` 관례대로 `((...) => void) | undefined` 로 쓴다.
- **기존 주석 보존.** 기능과 무관한 인라인 주석·설계 설명을 지우지 않는다.
- **`class-validator` 금지.** 검증은 Zod 로만 한다 (이번 작업엔 새 DTO 가 없다).
- **상수 값** (스펙에서 그대로 옮김): `ROW_HEIGHT = 32`, `INDENT_PX = 16`, `LABEL_BASE_PX = 8`, `MAX_TREE_DEPTH = 10`, 드래그 임계값 `3px`, 자동 스크롤 경계 `32px`, 배지 오프셋 `오른쪽 12px / 아래 16px`.
- **DB 변경 없음.** 스키마·마이그레이션을 건드리지 않는다.
- **테스트 실행:** `pnpm -F @sam/web test` (vitest run). 단일 파일은 `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`.

---

## File Structure

| 파일 | 역할 |
|---|---|
| `apps/web/src/lib/treeDnd.ts` (신규) | 드롭 대상 계산 전부. DOM 의존성 없음. |
| `apps/web/src/lib/treeDnd.test.ts` (신규) | 위 모듈의 단위 테스트. |
| `apps/web/src/components/Timeline.tsx` (수정) | 드래그 핸들, 포인터 상태 기계, 삽입선, 커서 배지, 자동 스크롤. |
| `apps/web/src/pages/ProjectDetailPage.tsx` (수정) | `moveTo()` 한 곳으로 API 호출 통합. 드래그와 위/아래 버튼이 공유. |
| `apps/web/src/components/ParentPickerDialog.tsx` (수정) | 자체 판정 로직을 `treeDnd.ts` 것으로 교체. 화면 변화 없음. |

Task 1~4 는 `treeDnd.ts` 만 건드리며 순수 TDD 다. Task 5~8 이 UI 다. Task 9 는 다이얼로그 통합.

---

## Task 1: 드롭 깊이 범위 계산

**Files:**
- Create: `apps/web/src/lib/treeDnd.ts`
- Test: `apps/web/src/lib/treeDnd.test.ts`

**Interfaces:**
- Consumes: `TreeNode` (`apps/web/src/components/NodeTree.tsx` 에서 export. `NodeTreeItem` 에 `children: TreeNode[]` 가 붙은 타입)
- Produces:
  - `export const INDENT_PX = 16`
  - `export const LABEL_BASE_PX = 8`
  - `export function depthRangeAt(rows: TreeNode[], boundary: number): { minDepth: number; maxDepth: number }`
  - `export function depthFromX(rows: TreeNode[], boundary: number, x: number): number`

**배경:** `rows` 는 화면에 보이는 행 목록이다(접힌 GROUP 의 자손은 빠져 있다). `boundary` 는 `0 .. rows.length` 범위이며 `rows[boundary]` **바로 위**의 경계를 뜻한다. `boundary === rows.length` 는 마지막 행 아래다.

- [ ] **Step 1: 테스트 파일을 만들고 깊이 범위 테스트를 쓴다**

`apps/web/src/lib/treeDnd.test.ts` 를 새로 만든다. `node()` 헬퍼는 같은 폴더의 `ganttLayout.test.ts` 관례를 그대로 따른다.

```ts
import { describe, it, expect } from 'vitest';
import type { NodeTreeItem } from '@sam/shared';
import type { TreeNode } from '../components/NodeTree';
import { INDENT_PX, LABEL_BASE_PX, depthRangeAt, depthFromX } from './treeDnd';

function item(
  partial: Partial<NodeTreeItem> & { id: string; kind: NodeTreeItem['kind'] },
): NodeTreeItem {
  return {
    projectId: 'p1',
    parentId: null,
    title: partial.id,
    description: null,
    startAt: null,
    endAt: null,
    startAtEffective: null,
    endAtEffective: null,
    progress: 0,
    progressEffective: null,
    sortOrder: 1,
    depth: 0,
    createdById: 'u1',
    updatedById: 'u1',
    createdAt: '2026-01-01T00:00:00.000Z',
    updatedAt: '2026-01-01T00:00:00.000Z',
    ...partial,
  } as NodeTreeItem;
}

// rows 는 화면에 보이는 순서대로 나열한 평탄화 결과다.
// children 은 depthRangeAt/resolveTarget 이 쓰지 않으므로 빈 배열로 둔다.
function row(
  partial: Partial<NodeTreeItem> & { id: string; kind: NodeTreeItem['kind'] },
): TreeNode {
  return { ...item(partial), children: [] };
}

//   G      (GROUP, depth 0)
//     C1   (ITEM,  depth 1)
//   S      (ITEM,  depth 0)
const ROWS: TreeNode[] = [
  row({ id: 'G', kind: 'GROUP', depth: 0, sortOrder: 1 }),
  row({ id: 'C1', kind: 'ITEM', depth: 1, sortOrder: 1, parentId: 'G' }),
  row({ id: 'S', kind: 'ITEM', depth: 0, sortOrder: 2 }),
];

describe('depthRangeAt', () => {
  it('트리 맨 위 경계는 깊이 0 으로 고정된다', () => {
    expect(depthRangeAt(ROWS, 0)).toEqual({ minDepth: 0, maxDepth: 0 });
  });

  it('above 가 펼쳐진 GROUP 이면 그 그룹의 첫 자식까지 허용한다', () => {
    // boundary 1 = G 와 C1 사이. above=G(GROUP, d0), below=C1(d1)
    expect(depthRangeAt(ROWS, 1)).toEqual({ minDepth: 1, maxDepth: 1 });
  });

  it('above 가 ITEM 이면 그 ITEM 의 깊이가 상한이다', () => {
    // boundary 2 = C1 과 S 사이. above=C1(ITEM, d1), below=S(d0)
    expect(depthRangeAt(ROWS, 2)).toEqual({ minDepth: 0, maxDepth: 1 });
  });

  it('below 가 없는 마지막 경계는 최상위까지 내려갈 수 있다', () => {
    // boundary 3 = S 아래. above=S(ITEM, d0), below=없음
    expect(depthRangeAt(ROWS, 3)).toEqual({ minDepth: 0, maxDepth: 0 });
  });
});

describe('depthFromX', () => {
  it('가로 좌표를 들여쓰기 한 칸 단위로 읽어 범위 안으로 clamp 한다', () => {
    // boundary 2 는 0..1 허용
    expect(depthFromX(ROWS, 2, LABEL_BASE_PX)).toBe(0);
    expect(depthFromX(ROWS, 2, LABEL_BASE_PX + INDENT_PX)).toBe(1);
    expect(depthFromX(ROWS, 2, LABEL_BASE_PX + INDENT_PX * 5)).toBe(1); // 상한 clamp
    expect(depthFromX(ROWS, 2, -100)).toBe(0); // 하한 clamp
  });

  it('한 칸의 절반을 넘으면 다음 깊이로 반올림한다', () => {
    expect(depthFromX(ROWS, 2, LABEL_BASE_PX + INDENT_PX * 0.4)).toBe(0);
    expect(depthFromX(ROWS, 2, LABEL_BASE_PX + INDENT_PX * 0.6)).toBe(1);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: FAIL — `Failed to resolve import "./treeDnd"`

- [ ] **Step 3: `treeDnd.ts` 를 만들고 두 함수를 구현한다**

```ts
// 트리 드래그 앤 드롭의 드롭 대상 계산.
// 드롭 대상은 (행 경계 boundary, 깊이 depth) 한 쌍이다.
// 세로 위치가 어느 두 행 사이인지를, 가로 위치가 들여쓰기 깊이(= 부모)를 정한다.
// DOM 에 의존하지 않는다 — 좌표 보정은 호출부(Timeline.tsx)가 끝내서 넘긴다.
import type { TreeNode } from '../components/NodeTree';

/** 트리 한 단계당 들여쓰기 픽셀. 라벨 칸의 paddingLeft 계산과 같은 값이어야 한다. */
export const INDENT_PX = 16;
/** 깊이 0 의 왼쪽 여백. 라벨 칸의 paddingLeft 계산과 같은 값이어야 한다. */
export const LABEL_BASE_PX = 8;

/**
 * 경계 `boundary` 에서 고를 수 있는 깊이의 범위.
 * - 상한: 위 행이 GROUP 이면 그 자식이 될 수 있으므로 +1
 * - 하한: 아래 행의 깊이. 아래 행이 없으면(트리 끝) 최상위까지 뺄 수 있다.
 */
export function depthRangeAt(
  rows: TreeNode[],
  boundary: number,
): { minDepth: number; maxDepth: number } {
  const above = boundary > 0 ? rows[boundary - 1] : undefined;
  const below = rows[boundary];

  if (!above) return { minDepth: 0, maxDepth: 0 };

  const maxDepth = above.kind === 'GROUP' ? above.depth + 1 : above.depth;
  const minDepth = below ? below.depth : 0;
  // below 가 above 보다 깊을 수는 없지만(평탄화 순서상), 방어적으로 뒤집힌 범위를 막는다.
  return { minDepth: Math.min(minDepth, maxDepth), maxDepth };
}

/** 가로 좌표(라벨 칸 왼쪽 끝 기준)를 깊이로 읽고 허용 범위로 clamp 한다. */
export function depthFromX(rows: TreeNode[], boundary: number, x: number): number {
  const { minDepth, maxDepth } = depthRangeAt(rows, boundary);
  const raw = Math.round((x - LABEL_BASE_PX) / INDENT_PX);
  return Math.max(minDepth, Math.min(maxDepth, raw));
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: PASS (6 tests)

- [ ] **Step 5: 타입 검사**

Run: `pnpm -r typecheck`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add apps/web/src/lib/treeDnd.ts apps/web/src/lib/treeDnd.test.ts
git commit -m "feat(web): 트리 DnD 드롭 깊이 범위 계산 추가"
```

---

## Task 2: 이동 가능 판정 (`canDropInto`)

**Files:**
- Modify: `apps/web/src/lib/treeDnd.ts`
- Test: `apps/web/src/lib/treeDnd.test.ts`

**Interfaces:**
- Consumes: Task 1 의 `treeDnd.ts`. `MAX_TREE_DEPTH` (`@sam/shared` 에서 export, 값 10).
- Produces:
  - `export function descendantIdsOf(items: NodeTreeItem[], nodeId: string): Set<string>` — 자기 자신을 포함한다
  - `export function subtreeRelativeDepth(items: NodeTreeItem[], nodeId: string): number` — 자손이 없으면 0
  - `export function canDropInto(items: NodeTreeItem[], node: NodeTreeItem, parentId: string | null): { ok: boolean; reason?: string }`

**배경:** 지금 `ParentPickerDialog.tsx` 안에 같은 판정이 들어 있다. Task 9 에서 그쪽을 이 함수로 갈아끼운다. 사유 문구는 커서 배지에 그대로 뜨므로 스펙 3절 표의 문구를 정확히 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`treeDnd.test.ts` 의 import 에 `descendantIdsOf, subtreeRelativeDepth, canDropInto` 를 추가하고, 파일 끝에 이어 붙인다.

```ts
//   G          (GROUP, d0)
//     GC       (GROUP, d1)
//       L      (ITEM,  d2)
//   S          (ITEM,  d0)
const ITEMS: NodeTreeItem[] = [
  item({ id: 'G', kind: 'GROUP', depth: 0, sortOrder: 1 }),
  item({ id: 'GC', kind: 'GROUP', depth: 1, sortOrder: 1, parentId: 'G' }),
  item({ id: 'L', kind: 'ITEM', depth: 2, sortOrder: 1, parentId: 'GC' }),
  item({ id: 'S', kind: 'ITEM', depth: 0, sortOrder: 2 }),
];

function byId(id: string): NodeTreeItem {
  const found = ITEMS.find((n) => n.id === id);
  if (!found) throw new Error(`no such test node: ${id}`);
  return found;
}

describe('descendantIdsOf', () => {
  it('자기 자신을 포함해 자손을 모은다', () => {
    expect(descendantIdsOf(ITEMS, 'G')).toEqual(new Set(['G', 'GC', 'L']));
    expect(descendantIdsOf(ITEMS, 'L')).toEqual(new Set(['L']));
  });
});

describe('subtreeRelativeDepth', () => {
  it('자손이 없으면 0, 있으면 가장 깊은 자손까지의 상대 깊이', () => {
    expect(subtreeRelativeDepth(ITEMS, 'L')).toBe(0);
    expect(subtreeRelativeDepth(ITEMS, 'GC')).toBe(1);
    expect(subtreeRelativeDepth(ITEMS, 'G')).toBe(2);
  });
});

describe('canDropInto', () => {
  it('최상위로 옮기는 건 언제나 된다', () => {
    expect(canDropInto(ITEMS, byId('GC'), null)).toEqual({ ok: true });
  });

  it('자기 자신 밑으로는 못 옮긴다', () => {
    expect(canDropInto(ITEMS, byId('G'), 'G')).toEqual({
      ok: false,
      reason: '자기 하위로는 옮길 수 없습니다',
    });
  });

  it('자손 밑으로는 못 옮긴다', () => {
    expect(canDropInto(ITEMS, byId('G'), 'GC')).toEqual({
      ok: false,
      reason: '자기 하위로는 옮길 수 없습니다',
    });
  });

  it('ITEM 은 부모가 될 수 없다', () => {
    expect(canDropInto(ITEMS, byId('G'), 'S')).toEqual({
      ok: false,
      reason: '일반 항목에는 넣을 수 없습니다',
    });
  });

  it('서브트리 깊이를 더해 최대 깊이를 넘으면 거절한다', () => {
    // 깊이 8 짜리 GROUP 아래에 상대깊이 2 인 G 를 넣으면 9+2 = 11 > 10
    const deep: NodeTreeItem[] = [
      ...ITEMS,
      item({ id: 'D8', kind: 'GROUP', depth: 8, sortOrder: 9 }),
    ];
    expect(canDropInto(deep, byId('G'), 'D8')).toEqual({
      ok: false,
      reason: '최대 깊이 10단계를 넘습니다',
    });
  });

  it('없는 부모를 지정하면 거절한다', () => {
    expect(canDropInto(ITEMS, byId('G'), 'nope').ok).toBe(false);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: FAIL — `descendantIdsOf is not a function` 계열 에러

- [ ] **Step 3: 세 함수를 구현한다**

`treeDnd.ts` 상단 import 를 아래로 바꾸고,

```ts
import { MAX_TREE_DEPTH, type NodeTreeItem } from '@sam/shared';
import type { TreeNode } from '../components/NodeTree';
```

파일 끝에 이어 붙인다.

```ts
/** 자기 자신을 포함한 자손 id 집합. */
export function descendantIdsOf(items: NodeTreeItem[], nodeId: string): Set<string> {
  const out = new Set<string>([nodeId]);
  const stack = [nodeId];
  while (stack.length > 0) {
    const cur = stack.pop()!;
    for (const it of items) {
      if (it.parentId === cur && !out.has(it.id)) {
        out.add(it.id);
        stack.push(it.id);
      }
    }
  }
  return out;
}

/** 서브트리가 자기 아래로 몇 단계까지 뻗는지. 자손이 없으면 0. */
export function subtreeRelativeDepth(items: NodeTreeItem[], nodeId: string): number {
  const ids = descendantIdsOf(items, nodeId);
  const self = items.find((n) => n.id === nodeId);
  if (!self) return 0;
  let max = self.depth;
  for (const it of items) {
    if (ids.has(it.id) && it.depth > max) max = it.depth;
  }
  return max - self.depth;
}

/**
 * `node` 를 `parentId` 아래로 옮길 수 있는지.
 * 거절 사유 문구는 커서 배지와 부모 선택 다이얼로그가 그대로 쓴다.
 */
export function canDropInto(
  items: NodeTreeItem[],
  node: NodeTreeItem,
  parentId: string | null,
): { ok: boolean; reason?: string } {
  let parentDepth = -1;

  if (parentId !== null) {
    const parent = items.find((n) => n.id === parentId);
    if (!parent) return { ok: false, reason: '대상 그룹을 찾을 수 없습니다' };
    if (descendantIdsOf(items, node.id).has(parentId)) {
      return { ok: false, reason: '자기 하위로는 옮길 수 없습니다' };
    }
    if (parent.kind !== 'GROUP') {
      return { ok: false, reason: '일반 항목에는 넣을 수 없습니다' };
    }
    parentDepth = parent.depth;
  }

  const newDepth = parentDepth + 1;
  if (newDepth + subtreeRelativeDepth(items, node.id) >= MAX_TREE_DEPTH) {
    return { ok: false, reason: `최대 깊이 ${MAX_TREE_DEPTH}단계를 넘습니다` };
  }
  return { ok: true };
}
```

> 판정 순서에 주의한다. 자손 검사가 ITEM 검사보다 **먼저** 와야 한다. 자기 자신을 부모로 지정한 경우(`canDropInto(items, G, 'G')`)에 "자기 하위" 사유가 나와야 하는데, 순서가 뒤집히면 ITEM 인 노드를 자기 자신에 넣을 때 엉뚱한 사유가 나온다.

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: PASS (14 tests)

- [ ] **Step 5: 타입 검사**

Run: `pnpm -r typecheck`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add apps/web/src/lib/treeDnd.ts apps/web/src/lib/treeDnd.test.ts
git commit -m "feat(web): 트리 DnD 이동 가능 판정 추가"
```

---

## Task 3: 드롭 대상 확정 (`resolveTarget`)

**Files:**
- Modify: `apps/web/src/lib/treeDnd.ts`
- Test: `apps/web/src/lib/treeDnd.test.ts`

**Interfaces:**
- Consumes: Task 1 의 `depthRangeAt`, Task 2 의 `canDropInto`
- Produces:
  - `export interface DropTarget { boundary: number; depth: number; parentId: string | null; insertIndex: number; ok: boolean; reason?: string }`
  - `export function siblingsExcluding(items: NodeTreeItem[], parentId: string | null, excludeId: string): NodeTreeItem[]` — `sortOrder` 오름차순
  - `export function resolveTarget(rows: TreeNode[], items: NodeTreeItem[], node: NodeTreeItem, boundary: number, depth: number): DropTarget`
  - `export function sortOrderForTarget(target: DropTarget): number`
  - `export function changesParent(node: NodeTreeItem, target: DropTarget): boolean`

**배경 — 부모와 삽입 위치를 끌어내는 규칙 (스펙 3절):**

- `above` 가 없으면(`boundary === 0`): `parentId = null`, `insertIndex = 0`
- `depth === above.depth + 1`: `parentId = above.id`, `insertIndex = 0` (그 그룹의 첫 자식)
- 그 외: `above` 에서 부모를 타고 올라가 `depth` 에 있는 조상 `anchor` 를 찾고, `anchor` **뒤에** 끼운다. `parentId = anchor.parentId`.

`insertIndex` 는 목표 부모의 자식들을 `sortOrder` 순으로 세우고 **드래그 중인 노드를 뺀** 배열에서의 0-based 삽입 위치다. `anchor` 가 드래그 중인 노드 자신이면 그 배열에 없으므로, "자기보다 `sortOrder` 가 작은 형제의 수"(= 지금 자리)로 계산한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

import 에 `resolveTarget, siblingsExcluding, sortOrderForTarget, changesParent, type DropTarget` 를 추가하고 파일 끝에 이어 붙인다.

```ts
//   G          (GROUP, d0, sortOrder 1)
//     C1       (ITEM,  d1, sortOrder 1)
//     C2       (ITEM,  d1, sortOrder 2)
//   S          (ITEM,  d0, sortOrder 2)
const T_ITEMS: NodeTreeItem[] = [
  item({ id: 'G', kind: 'GROUP', depth: 0, sortOrder: 1 }),
  item({ id: 'C1', kind: 'ITEM', depth: 1, sortOrder: 1, parentId: 'G' }),
  item({ id: 'C2', kind: 'ITEM', depth: 1, sortOrder: 2, parentId: 'G' }),
  item({ id: 'S', kind: 'ITEM', depth: 0, sortOrder: 2 }),
];
const T_ROWS: TreeNode[] = T_ITEMS.map((n) => ({ ...n, children: [] }));
function tById(id: string): NodeTreeItem {
  const found = T_ITEMS.find((n) => n.id === id);
  if (!found) throw new Error(`no such test node: ${id}`);
  return found;
}

describe('siblingsExcluding', () => {
  it('sortOrder 순으로 정렬하고 자기 자신을 뺀다', () => {
    expect(siblingsExcluding(T_ITEMS, 'G', 'C1').map((n) => n.id)).toEqual(['C2']);
    expect(siblingsExcluding(T_ITEMS, null, 'zzz').map((n) => n.id)).toEqual(['G', 'S']);
  });
});

describe('resolveTarget', () => {
  it('트리 맨 위 경계는 최상위 맨 앞이다', () => {
    // S 를 최상위 첫 번째로
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('S'), 0, 0);
    expect(t).toMatchObject({ parentId: null, insertIndex: 0, ok: true });
    expect(sortOrderForTarget(t)).toBe(1);
  });

  it('GROUP 바로 아래 경계에서 한 단계 깊으면 그 그룹의 첫 자식이 된다', () => {
    // boundary 1 = G 와 C1 사이, depth 1 → G 의 첫 자식
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('S'), 1, 1);
    expect(t).toMatchObject({ parentId: 'G', insertIndex: 0, ok: true });
    expect(sortOrderForTarget(t)).toBe(1);
  });

  it('같은 경계라도 얕은 깊이를 고르면 부모가 달라진다', () => {
    // boundary 3 = C2 와 S 사이.
    // depth 1 → G 안, C2 뒤
    const deep = resolveTarget(T_ROWS, T_ITEMS, tById('S'), 3, 1);
    expect(deep).toMatchObject({ parentId: 'G', insertIndex: 2, ok: true });
    expect(sortOrderForTarget(deep)).toBe(3);
    // depth 0 → 최상위, G 뒤
    const shallow = resolveTarget(T_ROWS, T_ITEMS, tById('S'), 3, 0);
    expect(shallow).toMatchObject({ parentId: null, insertIndex: 1, ok: true });
    expect(sortOrderForTarget(shallow)).toBe(2);
  });

  it('트리 맨 끝 경계에서 깊이 0 이면 최상위 맨 뒤로 간다', () => {
    // boundary 4 = S 아래. C1 을 최상위 맨 뒤로
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('C1'), 4, 0);
    expect(t).toMatchObject({ parentId: null, insertIndex: 2, ok: true });
    expect(sortOrderForTarget(t)).toBe(3);
  });

  it('같은 부모 안에서 아래로 옮기는 위치를 맞게 계산한다', () => {
    // C1 을 boundary 3(C2 와 S 사이), depth 1 → G 안 C2 뒤.
    // 자기 자신을 뺀 형제는 [C2] 이므로 insertIndex 1
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('C1'), 3, 1);
    expect(t).toMatchObject({ parentId: 'G', insertIndex: 1, ok: true });
    expect(sortOrderForTarget(t)).toBe(2);
  });

  it('자기 하위로 가는 대상은 사유를 달고 ok=false 로 돌아온다', () => {
    // boundary 1 = G 와 C1 사이, depth 1 → 부모가 G. G 자신을 드래그 중이면 무효.
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('G'), 1, 1);
    expect(t.ok).toBe(false);
    expect(t.reason).toBe('자기 하위로는 옮길 수 없습니다');
  });

  it('깊이 초과 대상도 사유를 달고 ok=false 로 돌아온다', () => {
    const deepItems: NodeTreeItem[] = [
      item({ id: 'D8', kind: 'GROUP', depth: 8, sortOrder: 1 }),
      item({ id: 'D9', kind: 'ITEM', depth: 9, sortOrder: 1, parentId: 'D8' }),
      item({ id: 'BIG', kind: 'GROUP', depth: 0, sortOrder: 2 }),
      item({ id: 'BIGC', kind: 'ITEM', depth: 1, sortOrder: 1, parentId: 'BIG' }),
    ];
    const deepRows: TreeNode[] = deepItems.map((n) => ({ ...n, children: [] }));
    const bigNode = deepItems.find((n) => n.id === 'BIG')!;
    // boundary 1 = D8 과 D9 사이, depth 9 → 부모 D8. BIG 은 상대깊이 1 이라 9+1 = 10 >= 10
    const t = resolveTarget(deepRows, deepItems, bigNode, 1, 9);
    expect(t.ok).toBe(false);
    expect(t.reason).toBe('최대 깊이 10단계를 넘습니다');
  });
});

describe('changesParent', () => {
  it('부모가 바뀌는지로 표시 색을 가른다', () => {
    const same = resolveTarget(T_ROWS, T_ITEMS, tById('C1'), 3, 1); // G 안 그대로
    expect(changesParent(tById('C1'), same)).toBe(false);
    const moved = resolveTarget(T_ROWS, T_ITEMS, tById('C1'), 4, 0); // 최상위로
    expect(changesParent(tById('C1'), moved)).toBe(true);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: FAIL — `resolveTarget is not a function`

- [ ] **Step 3: 구현한다**

`treeDnd.ts` 끝에 이어 붙인다.

```ts
export interface DropTarget {
  /** 삽입선을 그릴 행 경계 (0 .. rows.length) */
  boundary: number;
  /** 삽입선 들여쓰기 = LABEL_BASE_PX + depth * INDENT_PX */
  depth: number;
  parentId: string | null;
  /** 자기 자신을 뺀 형제 배열에서의 0-based 삽입 위치 */
  insertIndex: number;
  ok: boolean;
  reason?: string;
}

/** 같은 부모를 가진 형제를 sortOrder 순으로, 드래그 중인 노드만 빼고 돌려준다. */
export function siblingsExcluding(
  items: NodeTreeItem[],
  parentId: string | null,
  excludeId: string,
): NodeTreeItem[] {
  return items
    .filter((n) => n.parentId === parentId && n.id !== excludeId)
    .sort((a, b) => a.sortOrder - b.sortOrder);
}

/** (경계, 깊이) 를 실제 부모와 삽입 위치로 바꾸고 이동 가능한지 판정한다. */
export function resolveTarget(
  rows: TreeNode[],
  items: NodeTreeItem[],
  node: NodeTreeItem,
  boundary: number,
  depth: number,
): DropTarget {
  const above = boundary > 0 ? rows[boundary - 1] : undefined;

  let parentId: string | null;
  let anchor: NodeTreeItem | undefined; // 이 노드 "뒤"에 끼운다. 없으면 맨 앞.

  if (!above) {
    parentId = null;
  } else if (depth === above.depth + 1) {
    // 위 행의 첫 자식이 된다.
    parentId = above.id;
  } else {
    // 위 행에서 부모를 타고 올라가 목표 깊이의 조상을 찾는다.
    let cur: NodeTreeItem | undefined = items.find((n) => n.id === above.id);
    while (cur && cur.depth > depth) {
      cur = cur.parentId ? items.find((n) => n.id === cur!.parentId) : undefined;
    }
    anchor = cur;
    parentId = anchor ? anchor.parentId : null;
  }

  const siblings = siblingsExcluding(items, parentId, node.id);
  let insertIndex: number;
  if (!anchor) {
    insertIndex = 0;
  } else if (anchor.id === node.id) {
    // 조상이 드래그 중인 노드 자신이면 위 배열에 없다. 지금 자리를 그대로 쓴다.
    insertIndex = siblings.filter((s) => s.sortOrder < node.sortOrder).length;
  } else {
    insertIndex = siblings.findIndex((s) => s.id === anchor!.id) + 1;
  }

  const verdict = canDropInto(items, node, parentId);
  const target: DropTarget = {
    boundary,
    depth,
    parentId,
    insertIndex,
    ok: verdict.ok,
  };
  if (verdict.reason !== undefined) target.reason = verdict.reason;
  return target;
}

/** 서버가 받는 1-based 삽입 위치. */
export function sortOrderForTarget(target: DropTarget): number {
  return target.insertIndex + 1;
}

/** 이 드롭이 부모까지 바꾸는지 (표시 색 sky/amber 를 가른다). */
export function changesParent(node: NodeTreeItem, target: DropTarget): boolean {
  return target.parentId !== node.parentId;
}
```

> `exactOptionalPropertyTypes: true` 때문에 `reason: verdict.reason` 을 객체 리터럴에 바로 넣으면 `string | undefined` 를 optional 프로퍼티에 대입할 수 없다는 에러가 난다. 위처럼 `!== undefined` 로 분기해 나중에 붙인다.

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: PASS (23 tests)

- [ ] **Step 5: 타입 검사**

Run: `pnpm -r typecheck`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add apps/web/src/lib/treeDnd.ts apps/web/src/lib/treeDnd.test.ts
git commit -m "feat(web): 트리 DnD 드롭 대상 확정 로직 추가"
```

---

## Task 4: 포인터 좌표 → 드롭 대상, 그리고 배지 문구

**Files:**
- Modify: `apps/web/src/lib/treeDnd.ts`
- Test: `apps/web/src/lib/treeDnd.test.ts`

**Interfaces:**
- Consumes: Task 3 의 `resolveTarget`, `changesParent`. `ROW_HEIGHT` (`apps/web/src/lib/ganttLayout.ts` 에서 export, 값 32).
- Produces:
  - `export function targetFromPointer(rows: TreeNode[], items: NodeTreeItem[], node: NodeTreeItem, x: number, y: number): DropTarget | null`
  - `export function describeDropTarget(items: NodeTreeItem[], node: NodeTreeItem, target: DropTarget): { text: string; tone: 'sky' | 'amber' | 'rose' }`
  - `export function appendSortOrder(items: NodeTreeItem[], parentId: string | null, excludeId: string): number`

**배경:** `y` 는 행 컨테이너 상단 기준(첫 행 위쪽이 0), `x` 는 라벨 칸 왼쪽 끝 기준이다. 둘 다 스크롤 보정이 끝난 값이며, 보정은 Task 6 에서 `Timeline.tsx` 가 한다.

행 `i` 의 위 절반(`y` 가 `i*32 ~ i*32+16`)이면 경계 `i`, 아래 절반이면 경계 `i+1` 이다. 범위를 벗어난 `y` 는 양 끝 경계로 clamp 한다.

**제자리**는 `null` 을 돌려준다 — 선도 배지도 그리지 않기 위해서다. 무효(`ok: false`)와 다르다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

import 에 `targetFromPointer, describeDropTarget, appendSortOrder` 를 추가하고 파일 끝에 이어 붙인다. `T_ITEMS` / `T_ROWS` / `tById` 는 Task 3 에서 만든 것을 그대로 쓴다.

```ts
describe('targetFromPointer', () => {
  const X_D0 = LABEL_BASE_PX;              // 깊이 0 을 가리키는 가로 좌표
  const X_D1 = LABEL_BASE_PX + INDENT_PX;  // 깊이 1

  it('행의 위 절반은 그 행 위 경계, 아래 절반은 아래 경계로 읽는다', () => {
    // rows: [G, C1, C2, S]. S 를 드래그 중.
    // C1 행(index 1)의 위 절반 → boundary 1 → depth 1 → G 의 첫 자식
    const up = targetFromPointer(T_ROWS, T_ITEMS, tById('S'), X_D1, 32 * 1 + 4);
    expect(up).toMatchObject({ boundary: 1, parentId: 'G', insertIndex: 0 });
    // C1 행의 아래 절반 → boundary 2 → depth 1 → G 안 C1 뒤
    const down = targetFromPointer(T_ROWS, T_ITEMS, tById('S'), X_D1, 32 * 1 + 28);
    expect(down).toMatchObject({ boundary: 2, parentId: 'G', insertIndex: 1 });
  });

  it('가로 위치로 깊이가 갈린다', () => {
    // C2 행(index 2)의 아래 절반 → boundary 3
    const deep = targetFromPointer(T_ROWS, T_ITEMS, tById('S'), X_D1, 32 * 2 + 28);
    expect(deep).toMatchObject({ parentId: 'G' });
    const shallow = targetFromPointer(T_ROWS, T_ITEMS, tById('S'), X_D0, 32 * 2 + 28);
    expect(shallow).toMatchObject({ parentId: null });
  });

  it('제자리면 null 을 돌려준다', () => {
    // C1(G 의 첫 자식)을 드래그 중. 자기 행 위 절반과 아래 절반 둘 다 제자리.
    expect(targetFromPointer(T_ROWS, T_ITEMS, tById('C1'), X_D1, 32 * 1 + 4)).toBeNull();
    expect(targetFromPointer(T_ROWS, T_ITEMS, tById('C1'), X_D1, 32 * 1 + 28)).toBeNull();
  });

  it('범위를 벗어난 세로 좌표는 양 끝 경계로 clamp 한다', () => {
    expect(targetFromPointer(T_ROWS, T_ITEMS, tById('S'), X_D0, -500)?.boundary).toBe(0);
    expect(targetFromPointer(T_ROWS, T_ITEMS, tById('C1'), X_D0, 99999)?.boundary).toBe(4);
  });
});

describe('describeDropTarget', () => {
  it('같은 부모면 순서만 알려주고 sky 다', () => {
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('C1'), 3, 1);
    expect(describeDropTarget(T_ITEMS, tById('C1'), t)).toEqual({
      text: '2번째로 이동',
      tone: 'sky',
    });
  });

  it('다른 부모 안으로 가면 부모 이름을 붙이고 amber 다', () => {
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('S'), 1, 1);
    expect(describeDropTarget(T_ITEMS, tById('S'), t)).toEqual({
      text: '"G" 안 1번째로 이동',
      tone: 'amber',
    });
  });

  it('최상위로 가면 최상위라고 적고 amber 다', () => {
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('C1'), 4, 0);
    expect(describeDropTarget(T_ITEMS, tById('C1'), t)).toEqual({
      text: '최상위 3번째로 이동',
      tone: 'amber',
    });
  });

  it('무효한 대상은 사유를 그대로 적고 rose 다', () => {
    const t = resolveTarget(T_ROWS, T_ITEMS, tById('G'), 1, 1);
    expect(describeDropTarget(T_ITEMS, tById('G'), t)).toEqual({
      text: '자기 하위로는 옮길 수 없습니다',
      tone: 'rose',
    });
  });
});

describe('appendSortOrder', () => {
  it('자기 자신을 뺀 자식 수 + 1 이다', () => {
    expect(appendSortOrder(T_ITEMS, 'G', 'C1')).toBe(2);
    expect(appendSortOrder(T_ITEMS, 'G', 'zzz')).toBe(3);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: FAIL — `targetFromPointer is not a function`

- [ ] **Step 3: 구현한다**

`treeDnd.ts` 상단 import 에 `ROW_HEIGHT` 를 추가한다.

```ts
import { ROW_HEIGHT } from './ganttLayout';
```

파일 끝에 이어 붙인다.

```ts
/**
 * 포인터 좌표를 드롭 대상으로 바꾼다.
 * - `y`: 행 컨테이너 상단 기준 (첫 행 위쪽이 0)
 * - `x`: 라벨 칸 왼쪽 끝 기준
 * 둘 다 스크롤 보정이 끝난 값이어야 한다.
 *
 * 제자리면 null 을 돌려준다. 무효(ok:false)와 달리 알릴 사유가 없어
 * 선도 배지도 그리지 않기 때문이다.
 */
export function targetFromPointer(
  rows: TreeNode[],
  items: NodeTreeItem[],
  node: NodeTreeItem,
  x: number,
  y: number,
): DropTarget | null {
  const rowIndex = Math.floor(y / ROW_HEIGHT);
  const withinRow = y - rowIndex * ROW_HEIGHT;
  const rawBoundary = withinRow < ROW_HEIGHT / 2 ? rowIndex : rowIndex + 1;
  const boundary = Math.max(0, Math.min(rows.length, rawBoundary));

  const depth = depthFromX(rows, boundary, x);
  const target = resolveTarget(rows, items, node, boundary, depth);

  // 제자리 판정: 부모가 그대로이고 삽입 위치가 지금 자리와 같다.
  if (target.parentId === node.parentId) {
    const current = siblingsExcluding(items, node.parentId, node.id).filter(
      (s) => s.sortOrder < node.sortOrder,
    ).length;
    if (target.insertIndex === current) return null;
  }
  return target;
}

/** 커서 배지에 띄울 문구와 톤. */
export function describeDropTarget(
  items: NodeTreeItem[],
  node: NodeTreeItem,
  target: DropTarget,
): { text: string; tone: 'sky' | 'amber' | 'rose' } {
  if (!target.ok) {
    return { text: target.reason ?? '여기로는 옮길 수 없습니다', tone: 'rose' };
  }
  const nth = sortOrderForTarget(target);
  if (!changesParent(node, target)) {
    return { text: `${nth}번째로 이동`, tone: 'sky' };
  }
  if (target.parentId === null) {
    return { text: `최상위 ${nth}번째로 이동`, tone: 'amber' };
  }
  const parent = items.find((n) => n.id === target.parentId);
  return { text: `"${parent?.title ?? '?'}" 안 ${nth}번째로 이동`, tone: 'amber' };
}

/** 어떤 부모의 맨 뒤에 붙일 때 쓸 1-based sortOrder. */
export function appendSortOrder(
  items: NodeTreeItem[],
  parentId: string | null,
  excludeId: string,
): number {
  return siblingsExcluding(items, parentId, excludeId).length + 1;
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `pnpm -F @sam/web test -- src/lib/treeDnd.test.ts`
Expected: PASS (32 tests)

- [ ] **Step 5: 타입 검사**

Run: `pnpm -r typecheck`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add apps/web/src/lib/treeDnd.ts apps/web/src/lib/treeDnd.test.ts
git commit -m "feat(web): 포인터 좌표 → 드롭 대상 변환과 배지 문구 추가"
```

---

## Task 5: `moveTo` 로 API 호출 통합

**Files:**
- Modify: `apps/web/src/pages/ProjectDetailPage.tsx` (`onMoveSibling` 함수, 286-307행 부근)

**Interfaces:**
- Consumes: Task 3 의 `siblingsExcluding`. 기존 `useMoveNode` 훅.
- Produces: `ProjectDetailPage` 안의 `moveTo(node, newParentId, newSortOrder): Promise<void>`. Task 8 에서 `Timeline` 의 `onMoveTo` prop 으로 넘긴다.

**배경:** 드래그와 행 액션 메뉴의 위/아래 버튼이 같은 함수를 타게 만든다. `NodeRowActionMenu` 의 prop 모양(`onMoveSibling(node, direction)`)은 그대로 두고, `ProjectDetailPage` 쪽 구현만 `moveTo` 를 부르는 얇은 어댑터로 바꾼다. 이 컴포넌트는 UI 테스트 설비가 없으므로 자동 테스트 없이 타입 검사와 수동 확인으로 검증한다.

- [ ] **Step 1: import 를 추가한다**

`apps/web/src/pages/ProjectDetailPage.tsx` 상단 import 목록에 넣는다.

```ts
import { siblingsExcluding } from '../lib/treeDnd';
```

- [ ] **Step 2: `onMoveSibling` 을 `moveTo` + 어댑터로 바꾼다**

286-307행의 `async function onMoveSibling(...)` 전체를 아래로 교체한다.

```ts
  // 노드 이동의 단일 진입점. 드래그 앤 드롭과 행 액션 메뉴의 위/아래 버튼이 함께 쓴다.
  // newSortOrder 는 "목표 부모의 자식 중 자기 자신을 뺀 1-based 삽입 위치" 다 (서버 시맨틱).
  async function moveTo(
    node: NodeTreeItem,
    newParentId: string | null,
    newSortOrder: number,
  ): Promise<void> {
    await moveNode.mutateAsync({
      id: node.id,
      body: {
        newParentId,
        newSortOrder,
        expectedUpdatedAt: node.updatedAt,
      },
    });
  }

  // 위/아래 한 칸 이동. 방향을 삽입 위치로 환산해 moveTo 로 넘긴다.
  async function onMoveSibling(node: NodeTreeItem, direction: -1 | 1) {
    const siblings = siblingsExcluding(nodes.data ?? [], node.parentId, node.id);
    // 자기 자신을 뺀 배열에서 지금 자리
    const current = siblings.filter((s) => s.sortOrder < node.sortOrder).length;
    const nextIndex = current + direction;
    if (nextIndex < 0 || nextIndex > siblings.length) return;
    try {
      await moveTo(node, node.parentId, nextIndex + 1);
    } catch (err) {
      toast.error(apiErrorMessage(err));
    }
  }
```

> `moveTo` 자체는 예외를 삼키지 않는다. 호출부(어댑터와 Task 8 의 드래그 핸들러)가 각자 토스트를 띄우고, 드래그 쪽은 성공했을 때만 접힌 그룹을 펼쳐야 하기 때문이다.

- [ ] **Step 3: 위/아래 버튼이 그대로 동작하는지 확인한다**

Run: `pnpm -r typecheck`
Expected: 에러 없음

개발 서버가 떠 있다면 브라우저에서 행 액션 메뉴의 "위로/아래로"를 눌러 순서가 한 칸씩 바뀌는지, 맨 위/맨 아래에서 버튼이 비활성인지 확인한다. (`pnpm dev` 실행 전에 `AGENTS.md` 3절의 부팅 순서를 지킬 것.)

- [ ] **Step 4: 기존 테스트가 깨지지 않았는지 확인한다**

Run: `pnpm -F @sam/web test`
Expected: 전부 PASS

- [ ] **Step 5: 커밋**

```bash
git add apps/web/src/pages/ProjectDetailPage.tsx
git commit -m "refactor(web): 노드 이동 API 호출을 moveTo 한 곳으로 통합"
```

---

## Task 6: 드래그 핸들과 포인터 상태 기계

**Files:**
- Modify: `apps/web/src/components/Timeline.tsx`

**Interfaces:**
- Consumes: Task 4 의 `targetFromPointer`, Task 5 의 `moveTo` (prop 으로 받는다)
- Produces: `Timeline` 의 새 prop `onMoveTo`, 내부 상태 `nodeDrag`, `dropTarget`, ref `rowsRef`

**배경:** 이 파일에는 이미 마우스 드래그가 둘 있다 — 간트 막대 편집(`startBarDrag`, 474행 부근)과 배경 가로 패닝(`handleMouseDown`, 440행 부근). 새 드래그는 **핸들에서 시작한 `pointerdown` 만** 받으므로 둘과 겹치지 않는다. 핸들의 `onPointerDown` 에서 `e.stopPropagation()` 을 반드시 호출해 배경 패닝이 같이 시작되지 않게 한다.

이 태스크에서는 이동이 실제로 되는 것까지만 만든다. 삽입선과 배지는 Task 7 이다.

- [ ] **Step 1: import 와 prop 타입을 추가한다**

상단 import 에 추가한다.

```ts
import { targetFromPointer, sortOrderForTarget, type DropTarget } from '../lib/treeDnd';
```

`interface Props` 의 `onChangeParent` 줄 아래에 추가한다.

```ts
  // 드래그 앤 드롭으로 노드를 옮긴다. 성공 후 접힌 그룹을 펼쳐야 해서 Promise 를 받는다.
  onMoveTo?: ((node: NodeTreeItem, newParentId: string | null, newSortOrder: number) => Promise<void>) | undefined;
```

`TimelineComponent` 의 구조분해 목록에서 `onChangeParent,` 아래에 `onMoveTo,` 를 추가한다.

- [ ] **Step 2: 드래그 상태와 rows 컨테이너 ref 를 만든다**

`const [collapsedIds, setCollapsedIds] = useState<Set<string>>(new Set());` (132행 부근) 아래에 추가한다.

```ts
  // 트리 행 드래그(순서/부모 변경). 간트 막대 편집(barDrag), 배경 패닝(isDragging)과 별개다.
  const [nodeDrag, setNodeDrag] = useState<{ nodeId: string; active: boolean } | null>(null);
  const [dropTarget, setDropTarget] = useState<DropTarget | null>(null);
  const rowsRef = useRef<HTMLDivElement>(null);
```

- [ ] **Step 3: 행 목록을 ref 달린 컨테이너로 감싼다**

`{flat.map((n) => { ... })}` 블록(918-965행 부근)을 `<div ref={rowsRef} className="relative">...</div>` 로 감싼다. 오늘선(970행 부근)은 이 컨테이너 **밖에** 그대로 둔다 — 바깥 `relative` 컨테이너 기준으로 `top-0 bottom-0` 을 쓰고 있기 때문이다.

- [ ] **Step 4: 드롭 대상 계산용 행 목록을 만든다**

`const flat = useMemo(...)` (184-208행) 아래에 추가한다. `flat` 끝에는 "최상단 일정 추가..." 가짜 행이 붙어 있는데, 그건 트리 노드가 아니라 드롭 대상 계산에서 빼야 한다.

```ts
  // 드롭 대상 계산용. 마지막의 "최상단 일정 추가..." 가짜 행은 트리 노드가 아니므로 뺀다.
  const dragRows = useMemo(() => flat.filter((n) => n.id !== 'empty-row-placeholder'), [flat]);
```

- [ ] **Step 5: 드래그 시작 함수를 만든다**

`startBarDrag` 함수(474행 부근) 아래에 추가한다.

```ts
  // 라벨 칸의 ∷ 핸들에서만 시작한다. 3px 넘게 움직여야 실제 드래그로 전환.
  const startNodeDrag = (node: TreeNode, e: React.PointerEvent) => {
    e.preventDefault();
    e.stopPropagation(); // 배경 가로 패닝이 함께 시작되지 않게
    const startX = e.clientX;
    const startY = e.clientY;
    setNodeDrag({ nodeId: node.id, active: false });

    const computeTarget = (clientX: number, clientY: number): DropTarget | null => {
      const rowsEl = rowsRef.current;
      if (!rowsEl) return null;
      const rect = rowsEl.getBoundingClientRect();
      // x 는 라벨 칸 왼쪽 끝 기준. 라벨 칸은 sticky left-0 이라 스크롤러 왼쪽 끝과 같다.
      const scroller = scrollerRef.current;
      const labelLeft = scroller ? scroller.getBoundingClientRect().left : rect.left;
      return targetFromPointer(dragRows, items, node, clientX - labelLeft, clientY - rect.top);
    };

    const onMove = (ev: PointerEvent) => {
      const moved = Math.abs(ev.clientX - startX) + Math.abs(ev.clientY - startY);
      setNodeDrag((prev) => {
        if (!prev) return prev;
        if (!prev.active && moved < 3) return prev;
        return prev.active ? prev : { ...prev, active: true };
      });
      if (moved < 3) return;
      setDropTarget(computeTarget(ev.clientX, ev.clientY));
    };

    const finish = (ev: PointerEvent | null) => {
      window.removeEventListener('pointermove', onMove);
      window.removeEventListener('pointerup', onUp);
      window.removeEventListener('pointercancel', onCancel);
      window.removeEventListener('keydown', onKey);
      const target = ev ? computeTarget(ev.clientX, ev.clientY) : null;
      setNodeDrag(null);
      setDropTarget(null);
      return target;
    };

    const onUp = (ev: PointerEvent) => {
      const moved = Math.abs(ev.clientX - startX) + Math.abs(ev.clientY - startY);
      const target = finish(moved < 3 ? null : ev);
      if (!target || !target.ok || !onMoveTo) return;
      void onMoveTo(node, target.parentId, sortOrderForTarget(target))
        .then(() => {
          // 접힌 그룹 안으로 넣었으면 결과가 안 보이므로 펼쳐준다.
          if (target.parentId) {
            setCollapsedIds((prev) => {
              if (!prev.has(target.parentId!)) return prev;
              const next = new Set(prev);
              next.delete(target.parentId!);
              return next;
            });
          }
        })
        .catch(() => {
          /* 에러 토스트는 호출부(ProjectDetailPage)가 띄운다 */
        });
    };

    const onCancel = () => finish(null);
    const onKey = (ev: KeyboardEvent) => {
      if (ev.key === 'Escape') finish(null);
    };

    window.addEventListener('pointermove', onMove);
    window.addEventListener('pointerup', onUp);
    window.addEventListener('pointercancel', onCancel);
    window.addEventListener('keydown', onKey);
  };
```

> `targetFromPointer` 의 첫 인자는 **화면에 보이는 행 목록**(`dragRows`), 둘째는 **전체 노드**(`items`)다. 접힌 GROUP 의 자손은 `dragRows` 에 없지만 부모를 타고 올라가는 계산에는 필요하므로 둘 다 넘긴다.

- [ ] **Step 6: 핸들을 그린다**

`Row` 의 함수 시그니처(995행 부근)와 props 타입에 두 개를 추가한다.

```ts
  canDrag,
  onDragStart,
```

```ts
  canDrag: boolean;
  onDragStart: (node: TreeNode, e: React.PointerEvent) => void;
```

라벨 칸 안, 체크박스 블록(1108행 부근) **바로 앞**에 넣는다.

```tsx
        {/* 드래그 핸들: 여기서 시작한 pointerdown 만 노드 이동 드래그로 취급한다.
            자리는 항상 차지하고 hover 때만 보이게 해서 레이아웃이 흔들리지 않게 한다. */}
        <span
          className={`w-3 shrink-0 select-none text-slate-400 transition-opacity ${
            canDrag
              ? 'cursor-grab opacity-0 group-hover/row:opacity-100'
              : 'pointer-events-none opacity-0'
          }`}
          onPointerDown={(e) => {
            if (canDrag) onDragStart(node, e);
          }}
          title="드래그해서 순서나 상위 그룹을 바꿉니다"
          aria-hidden={!canDrag}
        >
          ⠿
        </span>
```

`<Row ... />` 호출부(930-963행)에 넘긴다.

```tsx
                  canDrag={!!canEdit && !selectionMode && n.id !== 'empty-row-placeholder' && !!onMoveTo}
                  onDragStart={startNodeDrag}
```

- [ ] **Step 7: 드래그 중인 행을 반투명하게 한다**

`Row` 의 props 에 `isDragging: boolean;` 을 추가하고, 최상단 `<div className="group/row flex ...">` (1095행 부근)의 className 뒤에 `${isDragging ? 'opacity-40' : ''}` 을 덧붙인다. 호출부에서 `isDragging={nodeDrag?.active === true && nodeDrag.nodeId === n.id}` 를 넘긴다.

- [ ] **Step 8: 타입 검사**

Run: `pnpm -r typecheck`
Expected: 에러 없음

- [ ] **Step 9: 커밋**

```bash
git add apps/web/src/components/Timeline.tsx
git commit -m "feat(web): 트리 행 드래그 핸들과 포인터 상태 기계 추가"
```

---

## Task 7: 삽입선과 커서 배지

**Files:**
- Modify: `apps/web/src/components/Timeline.tsx`

**Interfaces:**
- Consumes: Task 4 의 `describeDropTarget`, Task 6 의 `dropTarget` 상태
- Produces: 없음 (렌더링만 추가)

**배경:** 스펙 3절. 삽입선은 `LABEL_BASE_PX + depth * INDENT_PX` 에서 시작해 라벨 칸 오른쪽 끝까지 긋는다. 세로 위치는 `boundary * ROW_HEIGHT` 다. 색은 부모가 바뀌면 amber, 아니면 sky. 무효한 대상이면 선을 그리지 않고 배지에 사유만 띄운다.

- [ ] **Step 1: import 를 보강한다**

```ts
import {
  targetFromPointer,
  describeDropTarget,
  changesParent,
  INDENT_PX,
  LABEL_BASE_PX,
  type DropTarget,
} from '../lib/treeDnd';
```

- [ ] **Step 2: 커서 위치를 추적한다**

Task 6 의 `startNodeDrag` 안 `onMove` 끝에 한 줄 추가하고, 상태를 하나 더 만든다.

`nodeDrag` 상태 선언 옆에:

```ts
  const [dragCursor, setDragCursor] = useState<{ x: number; y: number } | null>(null);
```

`onMove` 안 `setDropTarget(...)` 다음 줄에:

```ts
      setDragCursor({ x: ev.clientX, y: ev.clientY });
```

`finish` 안 `setDropTarget(null);` 다음 줄에:

```ts
      setDragCursor(null);
```

- [ ] **Step 3: 삽입선을 그린다**

Task 6 Step 3 에서 만든 `<div ref={rowsRef} className="relative">` 안, `{flat.map(...)}` **다음**에 넣는다.

```tsx
            {/* 드롭 삽입선. 라벨 칸 안에만 그린다 — 라벨 칸은 sticky left-0 이라
                간트 영역을 가로로 스크롤해도 제자리에 남는다. */}
            {nodeDrag?.active && dropTarget?.ok && (
              <div
                className={`pointer-events-none absolute z-30 h-0.5 ${
                  dragNode && changesParent(dragNode, dropTarget) ? 'bg-amber-500' : 'bg-sky-500'
                }`}
                style={{
                  top: dropTarget.boundary * ROW_HEIGHT - 1,
                  left: LABEL_BASE_PX + dropTarget.depth * INDENT_PX,
                  width: Math.max(labelWidth - LABEL_BASE_PX - dropTarget.depth * INDENT_PX, 8),
                }}
              />
            )}
```

`dragNode` 는 `flat`/`items` 에서 찾아 쓴다. `dragRows` 선언 아래에 추가한다.

```ts
  const dragNode = useMemo(
    () => (nodeDrag ? items.find((n) => n.id === nodeDrag.nodeId) ?? null : null),
    [items, nodeDrag],
  );
```

- [ ] **Step 4: 커서 배지를 그린다**

컴포넌트 최상단 반환 JSX 의 맨 끝, `{hoveredNode && (<HistoryTooltip ... />)}` (979행 부근) 다음에 넣는다.

```tsx
        {/* 드래그 중 결과를 글로 알려주는 배지. 무효한 위치에서는 사유를 띄운다. */}
        {nodeDrag?.active && dragCursor && dragNode && dropTarget && (() => {
          const { text, tone } = describeDropTarget(items, dragNode, dropTarget);
          const toneClass =
            tone === 'rose'
              ? 'bg-rose-600 text-white'
              : tone === 'amber'
                ? 'bg-amber-500 text-white'
                : 'bg-sky-600 text-white';
          // 화면 오른쪽/아래 끝에 닿으면 반대쪽으로 뒤집는다.
          const flipX = dragCursor.x > window.innerWidth - 220;
          const flipY = dragCursor.y > window.innerHeight - 60;
          return (
            <div
              className={`pointer-events-none fixed z-50 max-w-xs truncate rounded px-2 py-1 text-xs shadow-lg ${toneClass}`}
              style={{
                left: flipX ? undefined : dragCursor.x + 12,
                right: flipX ? window.innerWidth - dragCursor.x + 12 : undefined,
                top: flipY ? undefined : dragCursor.y + 16,
                bottom: flipY ? window.innerHeight - dragCursor.y + 16 : undefined,
              }}
            >
              {text}
            </div>
          );
        })()}
```

- [ ] **Step 5: 무효한 위치에서 커서를 바꾼다**

스크롤러 `<div ref={scrollerRef} ...>` (780행 부근)의 className 조건식을 아래로 바꾼다.

```tsx
        className={`flex-1 min-h-0 overflow-auto rounded border border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-900 ${
          nodeDrag?.active
            ? dropTarget?.ok
              ? 'cursor-grabbing select-none'
              : 'cursor-not-allowed select-none'
            : isDragging
              ? 'cursor-grabbing select-none'
              : 'cursor-grab'
        }`}
```

- [ ] **Step 6: 타입 검사**

Run: `pnpm -r typecheck`
Expected: 에러 없음

- [ ] **Step 7: 브라우저에서 확인한다**

`pnpm dev` 로 띄우고 프로젝트 상세 화면에서:
- 행에 마우스를 올리면 왼쪽 끝에 `⠿` 이 나타난다
- 핸들을 끌면 원본 행이 흐려지고 삽입선이 따라온다
- 마우스를 좌우로 움직이면 삽입선의 들여쓰기가 바뀐다
- 부모가 바뀌는 위치에서 선이 amber, 같은 부모면 sky
- 자기 자손 안으로 끌면 선이 사라지고 배지가 rose 로 사유를 띄운다

- [ ] **Step 8: 커밋**

```bash
git add apps/web/src/components/Timeline.tsx
git commit -m "feat(web): 트리 DnD 삽입선과 커서 배지 표시"
```

---

## Task 8: 세로 자동 스크롤과 페이지 배선

**Files:**
- Modify: `apps/web/src/components/Timeline.tsx`
- Modify: `apps/web/src/pages/ProjectDetailPage.tsx`

**Interfaces:**
- Consumes: Task 5 의 `moveTo`, Task 6 의 `onMoveTo` prop
- Produces: 없음 (기능 완성)

**배경:** 긴 트리에서 화면 밖으로 옮기려면 자동 스크롤이 필요하다. 간트 막대 드래그가 이미 가로 자동 스크롤을 쓰고 있으므로(517행 부근) 같은 모양으로 세로를 만든다.

- [ ] **Step 1: `Timeline` 에 `onMoveTo` 를 넘긴다**

`ProjectDetailPage.tsx` 의 `<Timeline ... />` 에서 `onMoveSibling={onMoveSibling}` (542행 부근) 아래에 추가한다.

```tsx
              onMoveTo={canEditNodes ? async (node, newParentId, newSortOrder) => {
                try {
                  await moveTo(node, newParentId, newSortOrder);
                } catch (err) {
                  toast.error(apiErrorMessage(err));
                  throw err; // Timeline 이 성공했을 때만 접힌 그룹을 펼치도록
                }
              } : undefined}
```

- [ ] **Step 2: 세로 자동 스크롤을 붙인다**

`startNodeDrag` 안의 `onMove` 끝에 추가한다.

```ts
      // 스크롤러 위/아래 32px 안에 들어가면 세로로 자동 스크롤한다.
      const scroller = scrollerRef.current;
      if (scroller) {
        const box = scroller.getBoundingClientRect();
        const EDGE = 32;
        const MAX_SPEED = 12;
        let dy = 0;
        if (ev.clientY < box.top + EDGE) {
          dy = -MAX_SPEED * Math.min(1, (box.top + EDGE - ev.clientY) / EDGE);
        } else if (ev.clientY > box.bottom - EDGE) {
          dy = MAX_SPEED * Math.min(1, (ev.clientY - (box.bottom - EDGE)) / EDGE);
        }
        if (dy !== 0) scroller.scrollTop += dy;
      }
```

- [ ] **Step 3: 타입 검사와 전체 테스트**

Run: `pnpm -r typecheck && pnpm -F @sam/web test`
Expected: 에러 없음, 전부 PASS

- [ ] **Step 4: 스펙 8절의 수동 확인 목록을 모두 돌린다**

`pnpm dev` 로 띄우고 확인한다.

- MEMBER 계정으로 로그인하면 핸들이 안 보인다
- 체크박스 선택 모드에서 핸들이 사라진다
- 마우스를 가로로 움직이면 삽입선의 들여쓰기가 따라 바뀐다
- 부모가 바뀌면 amber, 안 바뀌면 sky
- 커서 배지가 커서를 따라오고, 화면 오른쪽/아래 끝에서 뒤집힌다
- 무효한 곳(자손 밑, 깊이 초과)에서 배지에 사유가 뜬다
- 접힌 GROUP 의 첫 자식으로 넣으면 이동 후 자동으로 펼쳐진다
- 드래그 중 `Esc` 로 취소된다
- 긴 트리에서 위/아래 끝으로 끌면 자동 스크롤이 걸린다
- 간트를 가로로 스크롤한 상태에서도 삽입선이 라벨 칸에 붙어 제자리에 그려진다
- 드래그 후 간트 막대, 지연 배지, GROUP 집계 날짜가 어긋나지 않는다

- [ ] **Step 5: 커밋**

```bash
git add apps/web/src/components/Timeline.tsx apps/web/src/pages/ProjectDetailPage.tsx
git commit -m "feat(web): 트리 DnD 자동 스크롤과 페이지 배선"
```

---

## Task 9: `ParentPickerDialog` 를 공용 판정으로 교체

**Files:**
- Modify: `apps/web/src/components/ParentPickerDialog.tsx`

**Interfaces:**
- Consumes: Task 2 의 `canDropInto`, Task 4 의 `appendSortOrder`
- Produces: 없음

**배경:** 다이얼로그와 드래그가 서로 다른 이유로 거절하기 시작하면 "왜 여긴 되고 저긴 안 되지"가 된다. 판정을 한 곳으로 모은다. 화면은 그대로다.

다만 다이얼로그에는 드래그에 없는 조건이 하나 있다 — **"현재 부모와 동일"이면 버튼을 비활성**한다. 이건 판정이 아니라 목록 UI 의 사정이므로 다이얼로그에 남긴다.

- [ ] **Step 1: import 를 바꾼다**

```ts
import { canDropInto, appendSortOrder } from '../lib/treeDnd';
```

`buildTree` 는 트리 렌더링에 계속 쓰므로 남긴다. `maxDescendantDepth` 임포트와 `subtreeMax` / `subtreeRelative` / `descendants` `useMemo` 블록(18-48행)은 `canDropInto` 안으로 흡수되었으므로 지운다.

- [ ] **Step 2: `isDisabled` 를 공용 판정에 위임한다**

50-67행의 `isDisabled` 를 아래로 바꾼다.

```ts
  function isDisabled(
    targetId: string | null,
    _targetDepth: number,
    _targetKind?: NodeTreeItem['kind'],
  ): { ok: boolean; reason?: string } {
    // 목록에서 현재 부모를 다시 고르는 건 의미가 없다. 이건 판정이 아니라 목록 UI 의 사정이라
    // 공용 canDropInto 가 아니라 여기서 본다.
    if (targetId === node.parentId) return { ok: false, reason: '현재 부모와 동일' };
    return canDropInto(items, node, targetId);
  }
```

- [ ] **Step 3: `computeAppendSortOrder` 를 지우고 공용 함수를 쓴다**

173-180행의 `function computeAppendSortOrder(...)` 를 통째로 지우고, `pick()` 안(77행)을 바꾼다.

```ts
      const newSortOrder = appendSortOrder(items, target?.id ?? null, node.id);
```

- [ ] **Step 4: 안내 문구의 깊이 계산을 고친다**

100-103행의 `<p>` 가 지워진 `subtreeRelative` 를 쓰고 있다. `subtreeRelativeDepth` 로 바꾼다. import 에 추가하고:

```ts
import { canDropInto, appendSortOrder, subtreeRelativeDepth } from '../lib/treeDnd';
```

컴포넌트 본문 위쪽에서 한 번 계산한다.

```ts
  const subtreeRelative = useMemo(() => subtreeRelativeDepth(items, node.id), [items, node.id]);
```

- [ ] **Step 5: 타입 검사와 전체 테스트**

Run: `pnpm -r typecheck && pnpm -F @sam/web test`
Expected: 에러 없음, 전부 PASS

- [ ] **Step 6: 다이얼로그가 그대로 동작하는지 확인한다**

브라우저에서 행 액션 메뉴 → "부모 그룹 변경"을 열고:
- GROUP 만 목록에 나온다
- 현재 부모, 자기 자신, 자손이 비활성이고 툴팁에 사유가 뜬다
- 고르면 그 그룹의 맨 뒤로 이동한다

- [ ] **Step 7: 커밋**

```bash
git add apps/web/src/components/ParentPickerDialog.tsx
git commit -m "refactor(web): 부모 변경 판정을 treeDnd 공용 함수로 통합"
```

---

## 완료 조건

- `pnpm -r typecheck` 통과
- `pnpm -F @sam/web test` 전부 통과 (`treeDnd.test.ts` 32건 포함)
- 스펙 8절 수동 확인 목록 전부 통과
- 백엔드 코드와 DB 스키마에 변경 없음
