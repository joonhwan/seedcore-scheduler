# 이관 문서 — 5차 완료 직후, 6차 착수 전 (2026-09-07)

저장소: `seedcore-scheduler`.

## 맨 먼저 볼 것 — 이 문서의 지시는 더 이상 유효하지 않습니다 (2026-09-07 오후)

**6차는 구현 없이 종결되었고, 이로써 요청 10건이 모두 완결되었습니다. 이 문서가 "6차에 할 일"로 적어 둔
네 갈래는 전부 빠졌습니다.** 아래 본문은 그 판단에 이르기까지 조사한 내용을 기록으로 남겨 둔 것이며,
각 절의 무효해진 서술은 취소선으로 표시했습니다.

**지금 유효한 요약은 `HANDOFF.md` §1 의 "6차 ✗ 구현 없이 종결" 절입니다.** 그것을 먼저 읽으십시오.

이 문서를 처음 쓴 뒤로 달라진 것은 넷입니다.

1. **5차는 `master` 에 머지·push 되었습니다** (`5e93ba0 Merge branch 'feat/server-notice'`, `origin/master` 와 동일).
   따라서 아래 "머지 전 스냅샷" 지시는 이미 지난 일입니다.
2. **간트 출력 세 갈래(③-나·다·라)가 빠졌습니다** (§2.0). **PDF 라이브러리를 들여올 일이 없습니다.**
3. **통합 로그(5-2)도 빠졌습니다.** 고객이 기존 프로젝트별 이력 화면(`/projects/:id/history`)을 직접 보고
   "이 기능은 추가할 필요가 없다"고 판단했습니다. **즉 6차에 남은 작업이 없습니다.**
4. **§5 의 미결 둘이 모두 종결되었습니다** (§5 참고).

> **이 문서가 통합 로그의 원천을 `audit_logs` 로 적은 것은 틀렸습니다.** 나중에 이 기능을 다시 검토할 일이
> 생기면 원천은 **`node_history`** 입니다. 요청 원문이 "누가 무슨 내용과 **어떤 부분을** 수정했는지"인데,
> 그 "어떤 부분"에 해당하는 정보가 `audit_logs` 에는 없고 `node_history.diffJson` 에 있습니다. 아울러
> `audit_logs` 에는 프로젝트로 묶는 색인이 없고 권한 검사 흐름도 없는 반면, `node_history` 는
> `@@index([projectIdSnapshot, occurredAt])` 와 `ProjectHistoryService.forProject` 의 멤버·adminMode 검사를
> 이미 갖추고 있습니다. 1차 검토 문서(`docs-customer/2026-09-01-요청사항-검토.md` §5-2)가 "시스템 기록도
> `audit_logs` 에 쌓이고 있다"고 덧붙인 **부연을 이 문서가 사양으로 잘못 옮긴 것**입니다. 자세한 대조는
> `HANDOFF.md` §1 의 6차 절에 있습니다.
>
> **요청 원문은 저장소에 없습니다.** 2026-09-01 세션 기록
> (`~/.claude/projects/D--workspace-prj-work-seedcore-scheduler/be52cdf7-….jsonl`)의 사용자 메시지에만
> 있습니다 — 요청 10건은 6째 줄, 고객 1차 회신 ①~⑥ 은 259째 줄입니다. 사양이 갈릴 때 추측하지 말고
> 그 원문을 읽으십시오.

<details>
<summary>옛 서술 — 5차가 아직 <code>master</code> 에 없다 (2026-09-07 오전 기준, 지금은 해소됨)</summary>

~~**5차(재시작 예고·접속자 목록)는 구현이 끝났지만 브랜치에 머물러 있습니다.**~~

```
브랜치: feat/server-notice
HEAD:   69f0d53
커밋:   15개 (a2bf162 = master 와의 분기점)
상태:   전체 시험 779건 통과, 세 워크스페이스 typecheck 통과, 작업 트리 깨끗
```

~~머지·PR 여부를 **사용자에게 확인받은 뒤** 진행하십시오. 그리고 **머지 전에 fly.io 볼륨 스냅샷을 뜨십시오**(AGENTS.md §7).~~ `master` 에 push 하면 컨테이너가 뜰 때 `prisma:migrate:deploy` 가 먼저 불려 마이그레이션이 자동 적용됩니다 — **이 사실은 그대로이므로 다음번 스키마 변경 때 다시 유효합니다.**

```bash
fly volumes list -a seedcore-scheduler
fly volumes snapshots create <volume id>
```

</details>

5차의 설계·계획·검증 기록은 아래에 있으므로 여기서 되풀이하지 않습니다. **구현 판단과 함정 요약은
`HANDOFF.md` §1 의 5차 절에 정리해 두었습니다**(2026-09-07 작성).

| 문서 | 담고 있는 것 |
|---|---|
| `docs/superpowers/specs/2026-09-07-재시작예고-접속자목록-design.md` | 5차 설계(확정 결정 넷, `@NoSessionTouch` 의 이유) |
| `docs/superpowers/plans/2026-09-07-재시작예고-접속자목록.md` | 5차 구현 계획(작업 열셋) |
| `.superpowers/sdd/2026-09-07-재시작예고-접속자목록/progress.md` | 작업별 검토 결과와 판단 아홉 건, 브라우저 실측 기록 (git 무시 대상이라 다른 PC 에는 없습니다) |

---

## 1. 요청 10건의 처리 상태

확정명세(`docs-customer/2026-09-01-요청사항-확정명세.md`)가 사양의 단일 원본이고 §9 에 진행 순서 표가 있습니다. ~~그 표의 5차 상태가 아직 "대기" 로 남아 있으니 갱신해야 합니다.~~ **갱신을 끝냈습니다**(§4). **아래 표의 10건이 이제 모두 종결 상태입니다.**

| 요청 | 내용 | 처리 |
|---|---|---|
| 1 | 사용자 삭제 | 3차 완료 |
| 2 | 로그인 유지 | 1차 완료 |
| 3 | MANAGER 전체 선택 → 사용자 그룹 | 2차 완료 |
| 4 | 목록 세로 스크롤 | 4차 완료 |
| 5-1 | 최근 업데이트순 정렬 | 1차 완료 |
| 5-2 | 수정 내역 통합 로그 팝업 | ~~6차 — 남음~~ → **종결 (2026-09-07). 기존 이력 화면으로 충족 확인** |
| 6 | 간트 확대·축소 | 4차 완료 (화면 배율만. **출력 셋은 2026-09-07 제외**, §2.0) |
| 7 | 수정일 갱신 | 1차 완료 |
| 8 | 접속자 확인 | 5차 완료 (2026-09-07) |
| 9 | 사용자 기준 일괄 권한 | 2차 완료 |
| 10 | 인원별 참여 프로젝트 | 2차 완료 |

---

## 2. 6차의 범위 — 없음 (2026-09-07 종결)

**네 갈래가 전부 빠져 6차에 만들 것이 남지 않았습니다.**

- ~~**5-2 전 프로젝트 통합 로그 팝업** — 프로젝트별 이력 화면은 이미 있고(`apps/web/src/pages/ProjectHistoryPage.tsx`), 전 프로젝트를 아우르는 통합 로그와 팝업 형태가 신규입니다.~~
  → **종결.** 고객이 그 프로젝트별 이력 화면을 직접 보고 "이 기능은 추가할 필요가 없다"고 판단했습니다.
  그 화면이 "누가·언제·어떤 일정의 어떤 부분을 고쳤는지"를 시간 역순으로 보여주고 기간(지난 1주/1달/직접
  지정)과 주제(전체·진척율 낮춤·삭제됨·기간 변경·댓글)로 걸러 볼 수 있어, 요청의 뼈대를 이미 담고
  있었습니다. **원 요청과 견주어 빠져 있던 것은 팝업 형태와 전 프로젝트 통합 둘뿐이며, 그 둘도 함께
  접었습니다.**

### 2.0 간트 출력(③-나·다·라) 제외 경위

| 갈래 | 처리 | 근거 |
|---|---|---|
| ③-나 "지금 화면 그대로" | 미구현 | 기존 내보내기를 실제로 써 본 사람이 지금 기능으로 충분하다고 전했다 |
| ③-다 기간 지정 출력 | 미구현 | 같음 |
| ③-라 분할 PDF | 미구현 | 고객이 "PDF 문서 출력은 안 해도 된다"고 명시적으로 알려왔다 |

- **㉱ 공식 회신은 오지 않았습니다.** 위 판단은 전언에 근거하며, 사용자가 2026-09-07 에 ③ 전체를 종결하기로 정했습니다.
- 그래서 고객이 읽는 확정명세에는 "완료"가 아니라 **③-나·다는 "기존 기능으로 충족 확인", ③-라는 "요청에 따라 제외"** 로 근거를 밝혀 적었습니다(확정명세 §3·§8·§9).
- **PDF 라이브러리 도입이 없습니다.** 이 문서가 6차의 유일한 새 의존성으로 적어 두었던 것이 사라져, 6차는 기존 구조 안의 작업만 남았습니다.
- ③-다(기간 지정 출력)는 기존 기능에 **없는** 것이므로, 나중에 필요해지면 확정명세 §3 에 사양이 그대로 남아 있습니다. 지우지 않았습니다.

<details>
<summary>옛 서술 — 출력 셋의 재료 조사 (구현하지 않으므로 무효. 되살릴 때만 참고)</summary>

~~**출력 쪽은 본뜰 것이 이미 다 있습니다.**~~

| 파일 | 역할 |
|---|---|
| `apps/web/src/lib/ganttExport.ts` | 순수 계산: `computeExportSize`, `collapsedIdsForDepth`, `treeMaxDepth`, `buildExportFilename`, 상수 `EXPORT_MAX_EDGE`(16000)·`EXPORT_PIXEL_RATIO`(2) |
| `apps/web/src/lib/ganttExport.test.ts` | 위 함수들의 시험 (본뜰 형태) |
| `apps/web/src/lib/exportGanttImage.ts` | `html-to-image` 의 `toPng` 로 화면 밖 간트를 찍는다 |
| `apps/web/src/lib/exportGanttExcel.ts` | `exceljs` 로 엑셀을 만든다 |
| `apps/web/src/components/GanttExportDialog.tsx` | 단위·트리 단계·테마를 고르는 대화상자 |
| `apps/web/src/components/GanttExportView.tsx` | 화면 밖에 그리는 간트 |
| `apps/web/src/components/ExportMenu.tsx` | 내보내기 메뉴 |

~~**PDF 라이브러리를 새로 들여와야 합니다.** 폐쇄망이라 화면이 외부에서 내려받는 방식은 쓸 수 없고, 개발 단계에서 번들에 함께 묶습니다. `exceljs` 가 이미 그 방식으로 들어와 있으므로 배포 구조는 달라지지 않습니다.~~

~~흐름은 "구간마다 `toPng` 로 이미지를 만들어 PDF 쪽에 넣는" 것이라, 검증된 이미지 내보내기 코드를 그대로 재사용합니다. `EXPORT_MAX_EDGE = 16000` 이 있는 이유를 확인하십시오 — 기간을 자르는 목적 자체가 이 한계와 인쇄 가독성입니다.~~

</details>

### 2.1 조사해 둔 재료 — 통합 로그를 나중에 되살릴 때만 참고

**구현하지 않으므로 아래는 무효입니다.** 다만 두 원천을 대조해 둔 것이라 나중에 이 기능이 다시 필요해지면
같은 조사를 되풀이하지 않도록 남깁니다. **원천은 `audit_logs` 가 아니라 `node_history` 입니다**(이 문서
머리의 정정 참고).

| | `node_history` (실제로 쓸 것) | `audit_logs` (이 문서가 잘못 적었던 것) |
|---|---|---|
| 담는 것 | 일정 변경만 (CREATE/UPDATE/MOVE/DELETE/RESTORE) | 시스템 전반 39종 (로그인·계정·권한·그룹·프로젝트·일정·예고) |
| 상세 | `diffJson` — **"어떤 부분을 어떻게" 가 여기 있습니다** | `payloadJson` — 있을 때도 없을 때도 있습니다 |
| 프로젝트로 묶기 | `projectIdSnapshot` + `@@index([projectIdSnapshot, occurredAt])` | `targetType`/`targetId` 뿐, 묶는 색인 없음 |
| 권한 검사 | `ProjectHistoryService.forProject` 가 멤버 여부·adminMode 를 봅니다 | 없음 |
| 화면·계약 | `ProjectHistoryQuery`/`ProjectHistoryEntry`(`packages/shared`), `ProjectHistoryPage.tsx`, `historyView.ts` 를 그대로 재사용 | 없음 |

- 기존 응답은 페이지를 나누지 않고 `ProjectHistoryResponse.truncated` 하나로 "더 있음"을 알립니다. 전 프로젝트로 넓히면 양이 훨씬 많아지므로 그 방식을 유지할지가 판단 지점이 됩니다.
- `audit_logs` 는 **읽는 기능이 아예 없습니다.** `apps/api/src` 의 어느 컨트롤러도 조회하지 않습니다(5차에서 감사로그를 확인할 때 Prisma Studio 로 DB 를 직접 열어야 했던 이유입니다). 감사로그 조회 화면을 별도 기능으로 만들 일이 생기면 그때는 **ADMIN 전용 여부를 먼저 정하십시오** — 다른 사람의 IP 와 행위가 들어 있어 일반 사용자에게 그대로 열 성격이 아닙니다.

### 2.2 시험용 데이터

`scripts/seed-test-projects.mjs` 가 기간이 **4개월(53건)·1년(66건)·3주(43건)** 로 다른 프로젝트를 만듭니다. ~~기간을 다르게 둔 것이 6차의 분할 출력이 길이에 따라 갈리기 때문입니다.~~ 그 이유는 사라졌으나, **일정이 많은 화면을 만들어야 할 때(사용설명서 스크린샷을 다시 찍는 일이 §3 에 남아 있습니다) 계속 쓸모가 있습니다.**

```bash
node scripts/seed-test-projects.mjs                       # admin/admin 기본
```

- 여러 번 돌리면 같은 것이 또 생깁니다(이름 중복을 걸러내지 않습니다).
- `scripts/cleanup-test-projects.mjs` 는 데모 하나만 남기고 지우므로 그것을 돌리면 여기서 만든 것도 사라집니다.

---

## 3. 배포 전에 남은 일 둘 — 이제 저장소에 남은 마지막 일

**6차가 종결되어 미룰 뒤 차수가 없습니다.** 아래 둘이 남은 전부이며, "6차까지 끝낸 뒤 한 번에" 라는 합의의
조건이 충족되었으므로 곧바로 착수할 수 있습니다.

1. **사용설명서(`docs/user-guide.md`)에 2·3·4차 기능이 아직 없고, 여기에 5차가 더해졌습니다.** 그룹 관리·사용자 상세·"퇴사자 포함" 보기·계정 정리가 §8.4 의 열거에서 빠져 있고, 목록 스크롤(§3.1)과 간트 배율(§5.2)은 옛 서술 그대로입니다. **스크린샷 두 장**(`docs/images/02_projects_list.png` 과 간트 화면)도 다시 찍어야 합니다. 사용자와 합의한 순서는 **6차까지 끝낸 뒤 한 번에 채우는 것**입니다(스크린샷을 두 번 찍지 않으려고).
2. **배포판 예행 연습이 최신 코드로 수행되지 않았습니다.** `dist-release/` 의 zip 은 커밋 `1b8cde6`(2026-09-03) 기준입니다. 사용자 요청으로 5차·6차를 끝낸 뒤로 미뤘습니다. 그때 확인할 것: 고객 서버에 **마이그레이션 두 개가 순서대로** 적용되는지(2차·3차의 `user_groups_and_retired_at` 과 5차의 `server_notices`), `sp-server.exe` 가 종료 코드 3으로 멈추는지, `sp-migrate.exe` 가 `backups/pre-migrate/` 에 스냅샷을 뜨고 행 수를 보존하는지.

   **fly.io 는 사정이 다릅니다.** 2차·3차 마이그레이션은 `origin/master` 에 2026-09-04 에 올라가 이미 적용되었고, **5차의 `server_notices` 도 2026-09-07 머지로 적용이 끝났습니다.** 고객 서버에는 아직 둘 다 적용되지 않았으므로, 두 환경의 상태가 다른 것을 헷갈리지 마십시오.

---

## 4. 문서 갱신 — 완료 (2026-09-07)

**아래 네 건은 모두 처리되었습니다.** 다시 손댈 필요가 없습니다.

- ~~확정명세 §9 표의 5차 상태가 "대기" 입니다.~~ → **"완료 (2026-09-07)" 로 고쳤습니다.** 6차 행도 범위 축소에 맞춰 "통합 로그 팝업 / 5-2" 로 좁혔습니다.
- ~~확정명세에 5차 완료 내용 절이 없습니다.~~ → **§9 에 "5차 완료 내용 (2026-09-07)" 절을 썼습니다.** 4차 절과 같은 형태입니다.
- ~~`HANDOFF.md` §1 에 5차 기록이 없습니다.~~ → **4차 절과 같은 밀도로 썼습니다.** 시험 중에 드러나 뒤에 고친 판단 열한 건이 그 절에 있습니다.
- ~~확정명세 §5-나와 달라진 점을 고객에게 전달해야 합니다.~~ → **§5-나에 "구현에서 달라진 점" 을 덧붙이고 §9 의 5차 완료 내용에도 다시 밝혔습니다.** 명세는 "**편집 중인 내용이 있으면** 저장하지 않은 변경이 있습니다를 함께 띄운다"인데, 구현은 **조건 없이 항상** 띄웁니다. 편집 상태를 아는 화면이 프로젝트 상세의 일정 편집 모달과 댓글 입력 둘뿐이라, 조건을 걸면 나머지 화면에서 안내가 조용해지기 때문입니다(설계 문서 §4.4).

여기에 더해 **③ 종결도 확정명세에 반영했습니다** — §0 머리말·요약 표, §3(절 머리의 처리 표와 갈래별 표기, ㉱ 종결 각주, PDF 기술 검토 무효 표기), §8 의 ㉱·㉲ 행, §9 표와 배포 계획입니다.

---

## 5. 사용자 판단을 기다리는 것 — 둘 다 종결 (2026-09-07)

- **㉱ (고객 회신 대기)** → **종결.** 공식 회신은 오지 않았으나, 실제로 써 본 사람이 지금 기능으로 충분하다고 전하고 고객이 PDF 출력은 필요 없다고 알려왔습니다. 사용자가 이를 근거로 **③ 전체를 종결**하기로 정했습니다(§2.0).
- **접속자 목록을 보는 관리자가 영구히 "접속 중" 인 것** → **종결. 이 문서의 서술이 애초에 낡은 것이었습니다.** 커밋 `5c1df69`("폴링하는 두 자리에 빠져 있던 가드 데코레이터를 채운다")가 5차 막바지에 `GET /admin/sessions/active` 에 `@NoSessionTouch` 를 붙였고, 부착을 못박는 시험도 `apps/api/src/sessions/admin-sessions.controller.test.ts` 에 있습니다. 이 문서를 쓴 시점(`1799c24`)이 그 커밋보다 앞섰습니다. 사용자에게 확인받은 방향("붙인다")과 코드가 이미 일치합니다.

---

## 6. 5차에서 알아낸 것 — 다음 차수에 쓸 것

- **Windows git-bash 에서 curl 로 한글 JSON 본문을 보내면 인코딩이 깨집니다.** 5차에서 curl 로 등록한 예고의 문구가 DB 에 `EFBFBD`(U+FFFD 대체 문자)로 저장되었습니다. 브라우저로 등록한 것은 정상 UTF-8 이었으므로 **애플리케이션은 문제가 없습니다.** 한글이 든 API 검증은 curl 대신 브라우저나 node 스크립트로 하십시오.
- **브라우저 자동화는 Playwright MCP 가 됩니다.** Chrome 확장(`claude-in-chrome`)은 연결되지 않았고, `.mcp.json` 의 Playwright 는 세션 시작 시 한 번 시간이 걸린 뒤 동작했습니다. 스냅샷과 스크린샷이 `.playwright-mcp/` 에 떨어집니다(git 무시 대상).
- **개발 DB 의 `admin` 비밀번호는 `admin` 입니다.**
- **마이그레이션 SQL 을 적용한 뒤에 손으로 고치면 Prisma 체크섬이 어긋납니다.** 5차에서 `CHECK` 를 손으로 더한 뒤 그 일이 생겨, `_prisma_migrations` 의 해당 행을 지우고 빈 테이블을 DROP 한 뒤 `prisma migrate deploy` 로 재적용해 맞췄습니다. **그대로 두면 다음번에 스키마를 바꿀 때 "migration has been modified" 로 거부됩니다.** 손으로 고칠 일이 생기면 적용 **전에** 고치거나, 고친 뒤 위 절차로 되돌리십시오.
- **`prisma migrate reset --force` 를 검증에 쓰지 마십시오.** 개발 DB 를 지워 시험 데이터가 함께 사라집니다. 임시 경로에 `DATABASE_URL` 을 걸고 `prisma migrate deploy` 를 돌리면 같은 검증을 안전하게 할 수 있습니다.
- **`@NoSessionTouch()` 패턴**: 사용자가 조작하지 않았는데도 주기적으로 오는 요청(폴링)이 `lastSeenAt` 을 올리면 접속자 판정이 무의미해집니다. 5차가 그 표시를 만들어 뒀으니(`apps/api/src/auth/auth.guard.ts`), **앞으로 폴링 라우트를 더할 때 이 표시를 붙일지 반드시 판단하십시오.** 붙이지 않아도 에러가 나지 않아 조용히 망가집니다. 부착 여부를 못 박는 시험이 `apps/api/src/server-notices/server-notices.controller.test.ts` 에 있습니다.
- **셸 heredoc 으로 한국어 여러 줄을 넘기면 이 환경에서 파싱이 깨집니다.** 커밋 메시지는 파일에 쓴 뒤 `git commit -F <경로>` 로 넘기십시오.

---

## 7. 작업 환경 주의사항

- **`prisma migrate dev` 는 개발 서버를 끄고 돌립니다.** SQLite 는 단일 writer 라 `pnpm dev` 나 Prisma Studio 가 DB 핸들을 쥐고 있으면 락이 걸립니다.
- **`packages/shared` 에 새 export 를 더하면 vite 사전 번들 캐시를 지워야 합니다.** 안 지우면 그 심볼이 조용히 `undefined` 가 되어 **에러 없이 화면만 빈 채로** 뜹니다. `pnpm dev` 로 띄우면 `scripts/dev.mjs` 가 매번 지웁니다.
- **API 는 3000 이 뜰 때까지 시간이 걸립니다.** `curl http://localhost:3000/api/v1/health` 가 200 을 줄 때까지 기다리십시오.
- **브라우저로 3000 을 직접 열지 마십시오.** exe 빌드가 남긴 `apps/api/public` 의 옛 화면이 뜹니다.
- 코드 수정 후 `pnpm -r typecheck`, 시험은 `pnpm -r test`(현재 779건).
- 셸에서 `cd` 를 독립적으로 쓰지 않고 절대 경로를 씁니다.

---

## 8. 권해 두는 스킬

**남은 일이 사용설명서와 배포 예행 연습 둘뿐이므로 권장 목록도 그에 맞게 줄었습니다.**

- **`mirero-tools:easy-korean-doc`** — **가장 자주 쓸 것입니다.** 고객이 읽는 문서를 다듬습니다. §4 의 확정명세 갱신은 끝났고, 다음 차례는 **사용설명서**(§3-1)입니다. 검사기가 절 제목과 어조 섞임까지 잡습니다.
- **`sam-troubleshooting`** (프로젝트 스킬) — `database is locked`, 옛 빌드 실행, 빈 화면 같은 함정. 배포 예행 연습에서 exe 를 검증할 때 특히 걸리기 쉽습니다.
- **`superpowers:verification-before-completion`** — 배포 예행 연습은 "됐다"고 말하기 전에 실제 출력을 확인해야 하는 작업입니다(마이그레이션 두 개의 적용 순서, 행 수 보존, 종료 코드 3).

<details>
<summary>옛 서술 — 6차 구현용 스킬 권장 (구현하지 않으므로 무효)</summary>

- ~~**`superpowers:brainstorming`** — 가장 먼저 부르십시오. 6차는 판단할 것이 여럿입니다: 통합 로그의 권한 범위(ADMIN 전용인지), PDF 라이브러리 선택, 분할 기준을 사용자가 고르는 방식, "지금 화면 그대로"가 배율만인지 펼침 상태까지인지. ㉱ 회신이 왔는지도 이 단계에서 확인하십시오.~~
- ~~**`superpowers:writing-plans`** — 설계를 승인받은 뒤에 부릅니다.~~
- ~~**`superpowers:test-driven-development`** — 기간 분할 계산은 경계가 많아 순수 함수로 떼어 시험하기에 알맞습니다.~~

**다만 아래 둘은 이 저장소에서 계속 유효합니다.**

- **`superpowers:subagent-driven-development`** — 5차를 이 방식으로 실행했고 잘 맞았습니다(작업마다 새 에이전트 + 검토). 계획에 실제 코드를 적어 두면 저비용 모델로도 정확히 옮깁니다.
- **`superpowers:systematic-debugging`** — 4차에서 프레임 단위로 재어 간트 깜박임의 원인을 잡았습니다. 추측으로 고치지 말고 재는 습관이 이 저장소에서 잘 통합니다.

</details>
