# 이관 문서 — SAM Scheduler 사용자 요청 대응 (2026-09-01)

작성 시점의 저장소: `D:\workspace\prj\work\seedcore-scheduler`, 브랜치 `master`, HEAD `58c0e75`.
작업 트리는 깨끗하고 원격(`origin/master`)에는 아직 push하지 않았다.

---

## 1. 지금까지의 경위

사용자 대표가 보낸 수정 요청 10건을 검토하고, 회신을 받아 사양을 확정한 뒤 1단계를 구현했다.
요구 명세·판정 근거·남은 질문은 모두 아래 두 문서에 있으므로 **여기서 반복하지 않는다.**

- `docs-customer/2026-09-01-요청사항-검토.md` — 10건의 1차 검토(구현 가능/불가 판정과 근거)
- `docs-customer/2026-09-01-요청사항-확정명세.md` — 회신 반영 확정 사양, DB 변경 총괄, 2차 확인 요청 문안, 진행 순서

구현 내용과 이유는 커밋 메시지에 상세히 적혀 있다.

- `8859151` feat: 로그인 12시간 유지·연장 창과 프로젝트 수정일 반영
- `f346bf9` docs: 사용설명서에 로그인 연장·수정일 안내 추가
- `58c0e75` docs: 사용설명서의 비밀번호 정책·계정 잠금 서술을 코드와 맞춤

아키텍처 결정 두 가지는 `AGENTS.md` §4.4(세션 수명)와 §4.5(프로젝트 수정일)에 규칙으로 못박아 두었다.
**새 세션은 이 두 절을 먼저 읽어야 한다.** 특히 다음 두 가지를 어기면 조용히 회귀한다.

- 세션 만료 시각을 바꾸는 곳은 **반드시 쿠키를 다시 발급**해야 한다(`login`, `extend` 두 곳뿐).
- 일정을 고쳤다고 `projects.updated_at` 을 건드리면 안 된다(낙관적 잠금 기준이라 409 유발).

---

## 2. 완료된 것 (1단계)

| 요청 | 결과 |
|---|---|
| 2번 로그인 유지 | 12시간 고정 + `POST /auth/extend` + 만료 10분 전 연장 창 + 헤더 남은 시간 배지 |
| 7번 수정일 갱신 | `ProjectListItem.lastScheduleChangeAt`(node_history 최신 시각). 댓글 제외 확인 |
| 5-1번 기본 정렬 | 최근 수정순으로 변경. 표시값과 정렬 기준을 `projectLastModifiedAt()` 하나로 통일 |

검증: `pnpm -r typecheck` 통과, API 272 + web 153 테스트 통과, 임시 DB로 실서버 띄워 쿠키 만료·연장·수정일
반영·댓글 제외·삭제 반영까지 확인함(임시 DB는 삭제).

부수적으로 마이그레이션 SQL이 작업 트리에 CRLF로 펼쳐져 checksum 테스트가 깨져 있던 것을 LF로 되돌렸다
(내용 변경 아님, 커밋에 포함되지 않음).

---

## 3. 남은 작업 — 다음 세션의 범위

우선순위와 확인 대기 항목은 확정명세 §9(개정된 진행 순서)가 단일 원본이다. 여기서는 착수 지점만 적는다.

### 2단계 — 화면 개선 (DB 변경 없음, 바로 착수 가능)

- **요청 4번: 프로젝트 목록 세로 스크롤.** `apps/web/src/pages/ProjectsPage.tsx` 의 `pageSize = 10`(132행 부근)과
  그 아래 페이지네이션 블록을 걷어내고 전체를 이어 보여준다. 표 머리글 고정 필요.
  1단계에서 기본 정렬을 수정일순으로 바꿨으므로, **이 작업을 해야 "편집하면 행이 다른 페이지로 사라진다"는
  옛 문제가 완전히 없어진다**(`lib/projectListSort.ts` 주석 참고).
- **요청 6번: 간트 배율 10% 단위.** `apps/web/src/components/Timeline.tsx` 의 `handleZoom()`(810행 부근)이
  1.3배씩 움직인다. 10% 단위로 바꾸고 현재 배율을 숫자로 표시하며 슬라이더를 함께 둔다.
  기존 화면 맞춤 버튼과 `+`/`-` 단축키는 유지한다.

### 3단계 — 사용자 그룹과 인원별 권한 관리 (테이블 2개 추가)

요청 ②(그룹), 3번(전체 선택), 9·10번(인원별 프로젝트 권한)이 한 덩어리다. 확정명세 §2와 §9·10 참고.

- 신규 테이블: `user_groups`, `user_group_members`(스키마 초안은 확정명세 §2에 있음)
- 서버에 **"이 사용자가 속한 프로젝트 목록"을 읽는 기능이 아직 없다**. `apps/api/src/members/` 는 프로젝트
  기준 API 뿐이다. 여기가 신규 작업의 중심이다.
- 지켜야 할 기존 규칙: 마지막 MANAGER 제거 금지, 모든 권한 변경은 감사 기록.
- **확인 대기 ㉯(그룹 인원 변경 시 기존 프로젝트 자동 반영 여부)가 이 단계의 설계를 좌우한다.** 회신 전에는
  명단 복사 방식(권장안)을 전제로 설계만 진행하고 구현은 보류하는 편이 안전하다.

### 4단계 — 사용자 정리 (컬럼 1개 추가)

요청 ⑥. `users.retired_at` 추가. 활동 기록이 0건인 계정만 완전 삭제 허용, 나머지는 퇴사 처리.
삭제 가능 여부 판정은 프로젝트 참여·일정·댓글·이력 네 곳을 모두 세어야 한다(FK가 전부 RESTRICT).

### 5단계 — 재시작 예고와 접속자 목록 (테이블 1개 추가)

요청 ⑤·8번. `server_notices` 추가. 접속자 목록은 기존 `sessions` 테이블로 충분하다
(1단계에서 `touch()` 가 `lastSeenAt` 을 계속 갱신하도록 남겨 두었다 — 이 용도다).
사용자 쪽은 5분 전부터 팝업, 확인 버튼, 1분 주기 재표시.

### 6단계 — 출력물과 통합 로그 (DB 변경 없음)

- **요청 ③ 기간 지정·분할 출력.** 단위별(일/주/월/분기) PNG·엑셀 내보내기는 **이미 있다**
  (`components/GanttExportDialog.tsx`, `ExcelExportDialog.tsx`). 없는 것은 ⑴ 기간을 잘라 뽑기,
  ⑵ 화면 배율 그대로 내보내기 두 가지다. `lib/ganttExport.ts` 의 `computeExportSize()` 가
  프로젝트 전체 범위(`computeActiveRange`)를 무조건 쓰는 것이 출발점이다.
- **요청 5-2번 통합 로그 팝업.** 감사 기록은 `audit_logs` 에 쌓이고 있으나 **읽는 기능이 없다**
  (`apps/api/src/audit/audit.service.ts` 는 쓰기 전용). 조회 API 신설이 필요하다.
  프로젝트 단위 이력 화면(`/projects/:id/history`)은 이미 있으므로 그것을 팝업으로 바꾸고
  전 프로젝트 통합 조회를 얹는 형태가 된다.

---

## 4. 사용자에게 회신을 기다리는 것

확정명세 §8에 전달용 문안이 있다. 여섯 가지(㉮ 연장 횟수 제한, ㉯ 그룹-프로젝트 연동 방식,
㉰ 복수 그룹 소속 허용, ㉱ 기존 내보내기 사용 경험, ㉲ 여러 장 출력물 수령 형태, ㉳ 접속자 기준 시간).

㉮는 1단계에서 "제한 없음"으로 이미 구현했으므로, 다른 답이 오면 그때 고치면 된다.
㉯는 3단계 착수 전에 반드시 받아야 한다.

---

## 5. 작업 환경 주의사항

- **개발 서버 절차**: `pnpm -F @sam/shared build` → `apps/web/node_modules/.vite` 삭제 → `pnpm dev`.
  가운데 캐시 삭제를 건너뛰면 shared 의 새 export 가 `undefined` 가 되어 **에러 없이 빈 화면**이 뜬다
  (AGENTS.md §5).
- **개발 중에는 브라우저로 5173 만 연다. 3000 을 직접 열면 옛 화면이 뜬다.**
  5173 은 `/api` 를 3000 으로 프록시하므로 3000 은 떠 있어야 하지만, 브라우저로 열 곳은 아니다.
  API 서버는 정적 화면 경로를 후보 순서대로 찾는데(`app.module.ts` 의 `resolveStaticRoot`),
  `apps/api/public` 이 `apps/web/dist` 보다 먼저 걸린다. 그 폴더는 `pnpm build:exe` 가
  `apps/web/dist` 를 복사해 만드는 산출물이고 git 무시 대상이라, **몇 달 전 exe 를 빌드한 흔적이
  남아 있으면 3000 에서 그 시절 화면이 그대로 뜬다.** 실제로 2026-09-01 에 이것 때문에
  "고친 기능이 반영되지 않는다"고 30분을 뒤졌다(코드·DB·API 는 모두 정상이었다).
  3000 에서 최신 화면을 보고 싶으면 `apps/api/public` 을 지운다 — 그러면 후보 순서상
  `apps/web/dist` 가 잡혀 `pnpm -F @sam/web build` 결과가 3000 에도 반영된다(서버 재시작 필요).
- **exe 검증은 개발 서버를 끄고 한다.** `sp-server.exe` 도 3000 을 쓰므로 `pnpm dev` 가 떠 있으면
  `EADDRINUSE` 로 죽는다. exe 는 실행한 폴더의 `data/sam.db` 를 쓰므로 개발 DB
  (`apps/api/prisma/data/app.db`)와 데이터가 별개다.
- Prisma Client 는 생성해 두었다. 1단계는 스키마 변경이 없었다. 3~5단계에서 마이그레이션이 생기면
  **작업 후 사용자에게 DB 변경 내용을 상세히 공유해야 한다**(AGENTS.md §7).
- `prisma migrate` 는 개발 서버를 끄고 돌린다(SQLite 단일 writer).
- 셸에서 `cd` 를 독립적으로 쓰지 않는다. 절대 경로를 쓴다(AGENTS.md §4.1).
- 커밋 메시지를 heredoc 으로 넘길 때 PowerShell 문법(`@'...'@`)을 Bash 도구에 쓰면 제목에 `@` 가
  섞인다. 파일에 써서 `git commit -F` 로 넘기는 편이 안전하다.

---

## 6. 권해 두는 스킬

- `superpowers:brainstorming` — 3단계(사용자 그룹)는 요구가 회신 과정에서 확대된 신규 기능이라,
  구현 전에 설계를 한 번 더 발라내는 편이 좋다. 특히 ㉯ 회신이 온 직후에 쓰기 적합하다.
- `superpowers:test-driven-development` — 4단계의 "삭제 가능 여부 판정"처럼 분기가 많은 순수 로직은
  테스트를 먼저 쓰는 편이 빠르다. 1단계에서도 `sessionCountdown.ts` 를 그렇게 떼어 두었다.
- `sam-troubleshooting` (프로젝트 스킬) — `database is locked`, 옛 빌드 실행, 빈 화면 같은 이 저장소
  고유의 함정을 다룬다. 개발 서버가 이상하게 굴면 먼저 본다.
- `mirero-tools:easy-korean-doc` — 사용자 대표에게 보낼 문서를 새로 쓰거나 고칠 때.
- `superpowers:verification-before-completion` — "됐다"고 보고하기 전에 실제로 돌려 본 증거를 남기는
  습관용. 1단계에서는 임시 DB에 서버를 띄워 쿠키 만료 시각까지 눈으로 확인했다.
